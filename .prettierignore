dist
dist-mac
docs
demo-vault
node_modules
src-tauri/target
src-tauri/gen
test-results
playwright-report
.claude
.vite
pnpm-lock.yaml
HardwareSoftware Security Engineering Tool
coverage

# conformance vectors are Rust-generated canonical bytes (M22.4) — byte-compared against regeneration
conformance/

# The shared policy artifacts (M24.1) are hashed, not read: policy.v1.json is
# byte-identity-checked against a committed digest that Rust and TS both
# assert, and every authority-routes snapshot IS the canonical bytes its
# filename hashes. A formatter that reflowed either would break the anchor
# that proves both languages loaded the same rules.
shared/policy/
