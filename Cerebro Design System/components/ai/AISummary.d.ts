/** Machine-generated content block — synapse tint, always with provenance. */
export interface AISummaryProps {
  /** default "AI summary" */
  title?: string;
  children?: React.ReactNode;
  /** provenance line, e.g. "From 6 signals · 4 teams" */
  sources?: string;
  onRegenerate?: () => void;
  style?: React.CSSProperties;
  className?: string;
}
export declare function AISummary(props: AISummaryProps): JSX.Element;
