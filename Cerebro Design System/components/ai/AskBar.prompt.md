Top-bar combined search + AI ask field. Sparkles glyph is synapse violet; one per app shell, centered.

```jsx
<AskBar onSubmit={q => runAsk(q)} />
<AskBar width={420} placeholder="Search or ask Cerebro" />
```
