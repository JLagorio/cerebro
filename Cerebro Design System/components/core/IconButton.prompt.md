Icon-only button for toolbars, panel headers, row actions. `label` is required (tooltip + aria).

```jsx
<IconButton icon="search" label="Search" />
<IconButton icon="settings-2" label="Board controls" variant="outline" />
<IconButton icon="x" label="Close" size="sm" />
<IconButton icon="star" label="Favorite" active />
```
