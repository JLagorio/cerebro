Owner/user identity. Initials on a deterministic muted background — no photos required.

```jsx
<Avatar name="Josef Lang" size={20} />
<AvatarGroup names={["Josef Lang","Ana Rios","Sam Ito","Mo Byrd"]} max={3} />
```
