name: This month
icon: calendar-days
color: null
order: 4
source:
  type: Work item
  project: null
filters: null
presentation:
  # M10: the month grid. A record appears on every day its span covers, which is
  # the whole reason a daterange is its own kind — a two-week window should read
  # as a band across the weeks, not as one chip on its start date.
  type: calendar
  dateField: window
  group: []
  sort:
    - field: due
      dir: asc
  columns:
    - field: status
    - field: assignee
