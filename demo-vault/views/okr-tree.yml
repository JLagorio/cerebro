name: OKR tree
icon: target
color: null
order: 1
source:
  type: Objective
  project: null
filters: null
presentation:
  type: tree
  groupBy: null
  orderBy:
    field: title
    dir: asc
  visibleFields:
    - status
    - owner
    - progress
    - key_result_count
  # Reverse descent: key results point UP at their objective, so the parent
  # needs no duplicate list of children (M3.5).
  childrenVia:
    type: Key result
    field: objective
