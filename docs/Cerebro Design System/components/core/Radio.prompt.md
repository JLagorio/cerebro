Radio for exclusive choices in forms/dialogs. For 2–5 short inline options prefer `SegmentedControl`.

```jsx
<Radio name="scope" checked label="My items" onChange={...} />
<Radio name="scope" label="Team items" onChange={...} />
```
