Styled native select for forms and board controls ("Group by", "Timeframe visualization").

```jsx
<Select width={200} value={v} onChange={e => setV(e.target.value)}
  options={[{value:"status",label:"Initiative status"},{value:"owner",label:"Owner"}]} />
```
