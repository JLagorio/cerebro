Workflow status chip; `bare` renders just the colored flag for table group headers.

```jsx
<StatusFlag status="progress" />
<StatusFlag bare status="planned" />
<StatusFlag label="Discovery" color="var(--swatch-magenta)" />
```
