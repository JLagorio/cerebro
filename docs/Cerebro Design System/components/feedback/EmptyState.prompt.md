Empty state per content-voice rules: declarative headline, one sentence, one action.

```jsx
<EmptyState icon="radar" title="No findings surfaced just yet"
  description="Cerebro spots recurring signals as knowledge accumulates."
  action={<Button variant="primary">Add knowledge</Button>} />
```
