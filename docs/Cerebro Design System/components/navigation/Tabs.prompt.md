Underline tabs — detail panel sections (Details / Spec / Insights / Health), settings pages.

```jsx
<Tabs active={tab} onChange={setTab} items={[
  {id:"details",label:"Details"},
  {id:"spec",label:"Spec"},
  {id:"insights",label:"Insights",count:3},
  {id:"health",label:"Health"}]} />
```
