# Cerebro M1 — Execution Log

Companion to [2026-07-24-cerebro-m1-foundation.md](2026-07-24-cerebro-m1-foundation.md) (the 24-task plan). Updated as tasks complete. A fresh session resumes from this file alone.

## Process

Subagent-driven development (superpowers:subagent-driven-development): per task — fresh implementer subagent → spec-compliance reviewer subagent → code-quality reviewer subagent; next task only when both reviews pass. Implementer prompts point at the plan's line ranges (below) plus lines 1–284 (conventions/contracts/coordination — binding). Branch: `m1-foundation` (never commit to main). After Task 24: final whole-implementation review, then superpowers:finishing-a-development-branch.

## Status

| Task | Lines in plan | Status | Commit |
|---|---|---|---|
| 1 Scaffold | 285–631 | ✅ done, spec ✓, quality ✓ | 56cb283 |
| 2 Tokens/fonts/@theme | 632–828 | ✅ done, spec ✓, quality ✓ | 2e72d31 |
| 3 DS primitives (17) | 829–1580 | ✅ done, spec ✓, quality ✓ | 5d36fe0 |
| — vitest glob infra fix | — | ✅ | 3901f1e |
| 4 Rust parser | 1581–2202 | ✅ done, spec ✓, quality ✓ (review found 2 Important + 6 Minor; 7 fixed, 1 deferred; fixes re-verified) | da50e24 + 8db3664 |
| 5 Rust scanner | 2203–2467 | ✅ done, combined review ✓ (all findings minor, deferred) | 2ef3226 |
| 6 Rust writes | 2468–2938 | ✅ done, combined review ✓ (2 Important fixed: fence-aware replace_h1 shared with parser; safe_join/safe_component path containment; fixes re-verified) | 02c59d4 + 9bc09c4 |
| 7 Config + command wiring | 2939–3214 | ✅ done, combined review ✓ (3 minors deferred) | 2608d1f |
| 8 Watcher | 3215–3557 | ✅ done, combined review ✓ (1 Important fixed: need_rescan overflow events now force emit; re-verified) | b952642 + 3fa722e |
| 9 Demo vault generator | 3558–4057 | ✅ done, combined review ✓ (scanner probe: 57/57 parse clean, 68/68 wikilinks resolve, deterministic) | 00fe705 |
| 10 ipc/mockParse/mockIpc | 4058–4763 | ✅ done, combined review ✓ (parity matrix 17/17 after fixes: createNote writes, 64 KB cap boundary, fence-aware setNoteTitle; re-verified byte-identical vs Rust) | e381405 + f86eaf3 + 58c7d9d |
| 11 vaultStore | 4764–5079 | ✅ done, combined review ✓ (1 Important fixed: createItem now rescans unconditionally — Tauri own-write suppression discards create events; + undefined→null normalization, watcher bind latch; re-verified) | 50f6b22 + 66b3d25 |
| 12 wikilink/normalize | 5080–5429 | ✅ done, combined review ✓ (clean; 3-way classifier parity verified) | 10cac1a |
| 13 schema | 5430–6008 | ✅ done, combined review ✓ (clean; real-vault probe 24/24, cycle-safe) | 71c97b7 |
| 14 grouping | 6009–6311 | ✅ done, combined review ✓ (verbatim; real-vault probes conserve all entries) | 57d11fc |
| 15 views/viewFilters | 6312–6805 | ✅ done, combined review ✓ (byte-verbatim vs plan; 24/24 round-trip, 59/59 adversarial no-throw, 9/9 demo-vault filter counts hand-verified; 1 Important plan-verbatim alias-cycle throw → note 13; minors deferred) | 4c874fb |
| 16 itemKeys/quickOpenScore | 6806–7026 | ✅ done, combined review ✓ (byte-verbatim; demo-vault next-keys hand-verified FLD-13/LNC-8/OPS-9/SYN-6; 3 minors deferred) | 1559cef |
| 17 stores + shell | 7027–7599 | ✅ done, spec ✓, quality ✓ (plan-verbatim + authorized toast deviation accepted; advisories → notes 14/15 + deferred polish; user-directed rail cleanup in fix commit) | 22f12d4 + 2a3e9d1 |
| 18 Sidebar + HomePage | 7600–8365 | ✅ done, spec ✓, quality ✓ (byte-verbatim except adjudicated plan-bug test fix — plan's `getByText('Product')` throws on 2 legit matches; demo-vault numbers hand-verified incl. done-group progress; greeting probe not time-flaky; advisories deferred) | c7d27a8 |
| 19 Space/Project/ViewToolbar | 8366–9059 | ✅ done, spec ✓, quality ✓ (note-13a cycle guard fixed + verified; 2 adjudicated deviations: collections.ts authored — confirmed plan gap, all demo-vault counts/orderings hand-verified; live-orderBy memo — plan-verbatim order select provably dead; advisories → note 16 + deferred) | b52cc04 + fc0cd02 |
| 20 ListView | 9060–9554 | ✅ done, spec ✓, quality ✓ (2 adjudicated deviations: root scroll wrapper — plan root omitted contract testid and no ancestor scrolls, live-DOM verified; quick-add guard per note 16a; group counts hand-verified in DOM incl. empty Cancelled section; advisories → note 17 + deferred) | d4cd485 |
| 21 BoardView | 9555–9838 | ✅ done, spec ✓, quality ✓ (3 adjudicated deviations all confirmed: plan's no-value `''` mapping provably dead — plan's own test fails against it; contract testid — plan root would have broken green suite; sanctioned `__none__` fixture; drag verified end-to-end incl. null-delete Rust parity; advisories → notes 17/18 + deferred) | 1139b91 |
| 22 DetailPanel | 9839–10363 | ✅ done, spec ✓, quality ✓ (note-18 fix verified TDD-first; 3 adjudicated deviations confirmed: Switch plan gap — byte-verbatim DS port, note-10 normalization idempotent, note-16a guards mutation-verified; fence-aware H1 rename live-tested with planted fake heading; 9 quality probes clean; advisories deferred; note 15(b) boot try/finally still owed → Task 23) | 868fa79 + 6196bf7 |
| 23 QuickOpen/CreateMenu/Settings/Toasts | 10364–11111 | ✅ done, spec ✓, quality ✓, fix round re-verified ✓ (notes 15a/b/c + 17 delivered with mutation-proven tests; STATUS_TEMPLATES byte-equivalent; D8 plan gap — sidebar New-project rows never wired by any task — resolved per note 14 by minimal wiring w/ space preselect; + double-create busy guard, toast z above dialog scrim; pre-fix worktree proved all 7 fix tests fail) | ae54d40 + a67557f + c794955 |
| 24 Playwright smoke | 11112–end | ✅ done, spec ✓, quality ✓ (config+smoke byte-verbatim; 4 deviations all confirmed plan-internal issues incl. group-header/list-group-header collision — plan specifies both; smoke 3/3 stable sub-second runs, poll-based, fresh mock per page load; baselines: 296 vitest / 66 cargo / 1 e2e / tsc 0; note 2 exclude merge verified) | 5eeca69 |

## Binding notes discovered during execution (feed to affected implementers)

1. **Task 6:** its planned test asserts `create_note` writes `"# Empty Note\n"` — must be `"# Empty note\n"`. Task 4 resolved a plan contradiction in favor of **sentence-case** `humanize_stem` (validated by spec review: coordination notes + parity fixtures + DS rules win over Task 4/6 literal text).
2. **Task 24:** `vite.config.ts` already has `exclude: [...configDefaults.exclude, '**/tolaria-main/**']` — Task 24's planned `exclude: ['e2e/**']` must be **merged into** that array, not replace it.
3. The vendored Tolaria reference now lives at `docs/tolaria-main/` (user moved it from repo root). It is gitignored by the `tolaria-main/` pattern at any depth.
4. Rust parity fixtures live in `src-tauri/src/vault/entry.rs` (not parse.rs); Task 10's mockParse.test.ts comment should point there.
5. Working tree has an uncommitted user edit appending `docs/` to `.gitignore` — leave it alone; flagged to user (it hides new files under docs/ from git, including future specs/plans).
6. Tauri icon `src-tauri/icons/icon.png` is required by `generate_context!` (plan gap, added in Task 1).
7. **Task 6:** `split_frontmatter`'s byte-reproduction round-trip invariant holds ONLY for LF-only, BOM-free files with a bare `---` closing fence (see doc comment at parse.rs:9-15). `update_frontmatter` must handle CRLF/BOM/trailing-whitespace-fence files deliberately (commit 8db3664 hardened the parser to accept them on read).
8. **Tasks 7/8:** vault writes are contained via `write.rs` `safe_join`/`safe_component` (rejects `..`/absolute/multi-segment); containment is LEXICAL — no canonicalization, so a user symlink inside the vault writes through to its target (accepted, standard for vault tools). The IPC command layer need not re-validate paths but must route ALL fs access through write.rs/scan.rs. `read_note` already exists in write.rs.
9. **Task 13:** demo-vault space status entries carry only `{id, group, color, hollow?}` — the seed `name` labels ("In progress", "Won't do") are dropped per the spec'd shape. `statusSetForSpace`/StatusDef labels must therefore be humanized from ids (e.g. `wontdo` → "Wontdo"); this is by design, not a generator bug.
10. **Task 22:** `readNote` leading-whitespace differs per backend: mock strips leading newlines (plan-spec'd), Rust `read_note` returns the body verbatim incl. the blank line after the fence. The description editor must tolerate both (e.g. trimStart on display) or Rust `read_note` gets aligned as deferred polish. Also `firstH1LineIndex` is exported from mockParse.ts (fence-aware H1 finder mirroring Rust `first_h1_line_start`).
11. **Tasks 15/20/21 (grouping advisories, spec-mandated):** (a) groupEntries' status column set comes from the FIRST entry's space (`statusSetForSpace(spaceForEntry(entries[0]))`) — cross-space view collections can flip board columns when sort order changes, and an unresolvable first entry falls back to DEFAULT_STATUSES demoting real statuses to ghosts; single-space project/space pages are safe. (b) The `__none__` group is emitted ONLY when non-empty — Board/ListView must rely on its trailing/pinned position only when present. (c) Multiselect groups by first value only; empty-string-in-array yields a blank-labeled group.
12. **Task 10 (done):** `mockParse.ts` must mirror parser behaviors added in 8db3664: fence-aware H1 extraction (skip ``` fenced regions and ≥4-space-indented lines), fence-aware snippet with post-strip-empty lines dropped (no double spaces), leading-BOM strip, CRLF tolerance (incl. empty frontmatter `---\r\n---\r\n`), trailing spaces/tabs allowed on the closing fence, and a 64 KB frontmatter cap → parse error. The 3 shared parity fixtures are unchanged.
13. **Task 19 (from Task 15 review, all plan-verbatim):** (a) `parseViewYaml` survives 59 adversarial-YAML cases but THROWS (stack overflow) on a self-referencing YAML alias inside `filters:` — the `parseFilterNode` recursion runs outside the try/catch that only guards `parse()`. Harden with a cycle/visited guard in a `fix:` commit before or during Task 19 (which feeds real user-editable `views/*.yml` from disk into the store). (b) Save-view UI must not permit empty/whitespace-only view names — `name: ''` serializes fine but parses back as the id (lossy round-trip). (c) A filter group whose rules are ALL malformed collapses to `{all: []}`, which matches EVERYTHING — silent match-all trap for Save-view UX.
14. **USER-DIRECTED DEVIATION (Tasks 18–24 + final review):** the Rail no longer has the plan's Docs/Agent/Library "Coming soon" placeholders (plan line 7327) — per user direction the rail is `c.` tile + Home + Settings only (commit 2a3e9d1; also fixed `icon="home"` → `"house"`, lucide 0.525 renamed the icon and the old name rendered an empty svg). Do NOT reintroduce them; Task 24's smoke has no rail assertions so nothing else changes. Product framing from the user: Cerebro is a note-taking tool structured like Tolaria (files-first vault); PM surfaces sit on top — avoid porting further decorative chrome from CerebroApp.dc.html that isn't functional in M1.
15. **Task 23 (from Task 17 quality review, plan-verbatim advisories):** (a) stale/missing last-vault boot lands in a dead empty shell — `openVault` sets `vaultPath` before scanning, `status:'error'` is displayed nowhere, and VaultChooser's error paragraph is unreachable (vaultPath set synchronously swaps to shell). SettingsPage must provide vault re-pick/recovery, and the shell (or Task 23 ToastHost era) should surface `vaultStore.status === 'error'`. (b) App.tsx boot IIFE has no `.catch` — a getLastVault rejection leaves `booted` false forever (blank screen); wrap in try/finally when touching App.tsx. (c) VaultChooser's async click handlers are unguarded (pickVault rejection → silent unhandled rejection).
16. **Tasks 20/21 (from Task 19 quality review):** (a) Save-view failure path is silent — `handleSaveView` has no try/catch and ViewToolbar fire-and-forgets it, closing the dialog immediately; a saveView/rescan rejection is an unhandled rejection and the user believes the view saved (note 15 class — surface via toast when touching this area). Same guard discipline applies to ListView quick-add / BoardView drag writes: `createItem` throws to callers by design. (b) collections' `optionOrder` ranks statuses from the first DECLARING entry's space while `groupEntries` columns derive from `entries[0]`'s space — for cross-space or untyped-first collections, list sort order and board columns can disagree (note 11 class; single-space Space/Project pages safe).
17. **Task 23 (from Task 20 quality review, plan-verbatim):** (a) an EMPTY project renders a blank canvas — `groupEntries([], …)` returns `[]` and Task 20's body dropped the placeholder's EmptyState, so a freshly-created project has no headers, no Add-item row, no empty state. This becomes user-reachable the moment Task 23's CreateMenu can create projects — fix as a ride-along by then (e.g. fall back to the flat "All items" group when groups come back empty, keeping quick-add reachable). BoardView renders the same blank canvas for an empty project (Task 21 review) — include it in the ride-along. (b) Apply note 16(a) guard discipline (catch → toast) to all CreateMenu/Settings write paths.
18. **Task 22 (fix owed, from Task 21 quality review, plan-verbatim):** before Task 22 proper, fix `handleDragEnd` (src/views/BoardView.tsx:48) via TDD in a separate `fix:` commit: when the grouped field's resolved def kind is person/relation, wrap the written value in `[[…]]` — the current bare-stem write destroys the wikilink on disk (probe: assignee-grouped drag wrote `assignee: dana-fox`; `relationships.assignee` gone after rescan; display self-heals via stem matching but backlink semantics and Obsidian interop silently degrade; reachable today via any saved view grouping by assignee). Multiselect drop clobbering the array (tags ['alpha','beta'] dropped on beta → `tags: 'beta'`) stays DEFERRED — proper add/remove semantics are M2 design.

## Deferred polish (end of M1, after Task 24)

- `@theme` stock-palette reset (`--color-*: initial; --radius-*: initial;`) to block off-DS utilities — apply only after all UI tasks land
- Dialog: Escape-to-close/focus-management ownership decision (consumers own it per plan-verbatim code for now)
- SegmentedControl ARIA (`role="tab"` + `aria-selected`); FilterChip remove-× keyboard access
- `bundle.icon` entry in tauri.conf.json; CSP hardening (`"csp": null` currently) before anything ships
- Consider @testing-library/jest-dom for later tests; App.test.tsx tautological assertion note
- Parser: non-string `type` frontmatter value (e.g. `type: 123`) is silently dropped from both entry_type and properties (plan-verbatim; Task 4 quality finding 6, deferred) — consider keeping it in properties
- Parser: within-cap YAML flow-nesting bombs (~40 KB) can still take ~3 s before erroring (bounded, acceptable)
- Scanner (spec-verbatim, Task 5 review): one unreadable/non-UTF8 .md aborts the whole scan (consider degrading to per-file parse_error); `.MD` uppercase skipped; symlinked notes skipped; `views/`/`attachments/` skipped at any depth; testutil temp dirs leak on failed asserts
- IPC layer (spec-verbatim, Task 7 review): sync commands (scan_vault etc.) run on main thread — stalls UI on large vaults, consider `#[tauri::command(async)]`; pick_vault discards picked folder if config persist fails; `to_string_lossy` on picked path
- Watcher (Task 8 review, minors): own-write registration races event delivery (worst case one spurious idempotent rescan); `.MD`/`.YML` case-sensitive; mutex poisoning silently disables suppression; no logging on emit/callback Err; ≤100ms double-emit window on watcher replace
- vaultStore (Task 11 review, minors): patchFrontmatter failures resolve silently — Task 17+ should surface write errors via toasts; interleaved slow-write transient clobber (self-healing, mock-unreachable); concurrent openVault calls could double-bind the vault-changed listener (duplicate idempotent rescans only)
- wikilink/normalize (Task 12 review, minors, plan-verbatim): normalizeFrontmatter flattens nested non-wikilink arrays to [null]; resolveTarget("x.md") never matches (targets are stems) — Task 13+ authors take note
- views/viewFilters (Task 15 review, minors, plan-verbatim): alias-cycle throw in parseViewYaml (see binding note 13 — fix due by Task 19); `obj.value as Scalar | Scalar[]` admits YAML maps/cyclic arrays into FilterRule.value (inert at eval time but poisons later JSON.stringify); before/after on array-valued fields compares only the first element; parseFilters/DEFAULT_PRESENTATION exported with no consumer yet (expected Tasks 19/collections)
- itemKeys/quickOpenScore (Task 16 review, minors, plan-verbatim): parseInt precision loss above 2^53 could yield a duplicate key (unreachable — keys are app-generated small ints); KEY_SHAPE `/^[a-z]+-\d+$/i` excludes digit-bearing prefixes (e.g. `A1-2`) from hyphen-less quick-open matching; stale `// substring` comment at quickOpenScore.test.ts:39 (case actually scores word-boundary tier)
- Smoke/e2e (Task 24 reviews, minors): `.gitignore` lacks `test-results/`/`playwright-report/` entries — first traced run dumps untracked dirs with commit-by-accident risk (BLOCKED on the user's uncommitted .gitignore edit — reconcile together); smoke's target-column pick doesn't exclude `__none__` (latent hard-timeout if a status-less demo item ever lands in column 2) and `sourceKey` is interpolated raw into a CSS selector (safe for slug keys only); dual board-column testids — inner `board-column-${id}` has zero consumers and a different key vocabulary than the root's `data-group-key` (delete or comment-justify); smoke's demo-button branch is dead in mock mode so chooser coverage rests on App.test alone; operational: reuseExistingServer + a long-lived dev server can e2e-test stale vite config after future commits, and local `retries: 0` makes any local flake a hard red; sweep the inline "Deviation (reported)" audit-trail comments (App/BoardView/DetailPanel/CreateMenu et al.) before M1 close
- Chrome (Task 23 reviews, minors, mostly plan-verbatim): ⌘K listener registers before boot completes — ⌘K on the vault chooser latches quickOpenVisible so the palette pops the moment a vault opens; `metaKey||ctrlKey` hijacks macOS Ctrl+K kill-line in textareas and Cmd+Shift+K matches too; VaultChooser comment claims store error displays there but that branch is dead (openVault routes errors to toast+Settings) — fix comment when touched; QuickOpen input lacks combobox semantics (no aria-activedescendant/aria-expanded) and no focus restore on close; CreateMenu dropdown lacks role=menu/Escape-close/aria-expanded, backdrop button sits between trigger and entries in tab order; fresh-vault dead-ends — NewItemDialog with zero projects / NewProjectDialog with zero spaces show an empty Select and permanently disabled Create with no ordering hint; keyboard users must Tab to Create (Enter doesn't submit dialogs — also what makes double-Enter safe); record-only: clicking a second New-project row while the dialog is open would keep the first space (unreachable — scrim intercepts); QuickOpen.test lacks Escape/backdrop-close and key-field ranking cases; CreateMenu success tests leak detailPath harmlessly
- DetailPanel (Task 22 reviews, minors/advisories): authored — `commitTitle` catch wraps both setNoteTitle AND rescan, so a rescan-only failure shows "Couldn't rename item" and reverts the input while disk holds the NEW name (separate the catches when convenient); plan-verbatim — Escape closes the whole panel discarding an in-progress description edit silently (popover should close first; textarea doesn't stopPropagation — the one reachable mid-edit data-drop path); stale-body-after-rename CONFIRMED consequence: rename then save of the stale textarea reverts the H1 on disk (readNote effect keys on entry.path only; proper refetch policy is M2 design); popover has no focus trap/return and bottom-opens extend the scroll area; number editor writes `.nan` for non-numeric drafts (add Number.isNaN guard); late save settle poisons savedBody → spurious re-write + "Saved" toast after item switch; undeclared object-valued properties render "[object Object]"; description keystrokes re-render the whole panel; DetailPanel re-renders on rescan even when closed (useSchema before early return); 2 act() warnings in the suite; test 1 permanently replaces a store method for its file; coverage gaps: successful rename, popover close paths, checkbox/date/text editors, Escape-mid-edit
- BoardView (Task 21 reviews, plan-verbatim/emergent minors): multiselect drop clobbers the whole array (see note 18 — deferred to M2 design); optimistic "Moved to X" toast fires before the write settles (failure shows both toasts in sequence); stale active id after mid-drag rescan → write attempt + success toast (self-heals via failure toast); ghost-column drop writes the raw out-of-schema value with raw toast label (sane, kept); a literal `"__none__"` frontmatter value would collide with the no-value key namespace (grouping key-namespace issue); groupEntries + parse-error filter recompute per render; no DragOverlay — source-transform can clip against the inner overflow-x row on far vertical drags; KeyboardSensor absent (cards get role=button/tabIndex from dnd-kit but Enter/Space inert); column headers lack heading semantics; test fixture marks `__none__` as ghost:true but grouping emits ghost:false; coverage gaps: ghost drop, stale-id two-toast pin, relation/multiselect write shape, click-opens-detail
- ListView/quick-add (Task 20 reviews, mostly plan-verbatim): double-Enter before createItem resolves mints two rows with the same key (no isSubmitting guard; two concurrent drafts in different groups also collide — key computed at submit from not-yet-rescanned entries); non-Latin/symbol-only title → `slugify` returns '' → mock silently writes `items/.md` with empty title while Rust rejects → toast (add `slug || key.toLowerCase()` fallback); authored-accepted: rescan-failure after a successful write lands in the "Couldn't create" catch — misleading toast + retry mints `-2` slug duplicate; rows are `role="row"` outside any grid with no tabIndex — keyboard users can't open items; header counts read "Todo3" to SRs; ghost group dot is shape/color-only; focus drops to body after submit; unresolvable person chip renders raw stem un-ghosted; duplicate visibleFields names collide chip keys; QuickAddRow subscribes to whole entries array; tests lack flat-list/double-submit/person-date-chip cases and the guard test pins draft presence not value; factories carry no `due` fixture
- collections/ViewToolbar (Task 19 reviews, minors): intransitive comparator when number and string values mix in one field (`estimate: 2` vs `"3h"` — numeric-coerce or `localeCompare(..., {numeric:true})`); O(N²) resolveTarget scans in resolveCollection (132–260ms per call at 2k entries, and the path-sorted scan order IS the adversarial order — build a byPath/project→space index when Tasks 20/21 make this hot); view case returns `view.definition.presentation` by reference while other cases clone; `slugifyViewId` collisions silently overwrite `views/<id>.yml`, symbol-only names collapse to id `view`; `orderToValue` displays unknown saved orderings as "Last modified" while sorting by the real field; toolbar Selects have no accessible name (DS Select lacks a label prop); one-frame stale sort/presentation flash on selection switch (plan-verbatim); filterless saved view = whole vault incl. type/space/person notes (M2 filter-builder territory); relation orderBy sorts by raw wikilink target where grouping uses resolved display names; collections tests lack desc-on-select/stability/cross-space pins and the "by-due" test actually sorts priority
- Sidebar/HomePage (Task 18 reviews, plan-verbatim advisories): collapse chevron is `span role="button"` nested inside a `button` — invalid nesting, mouse-only (no tabIndex/keydown/aria-expanded), inner aria-label pollutes the row name; orphaned projects (unresolvable space link) silently dropped from sidebar but shown on HomePage with "—" subtitle; `projectProgress` O(P×N²) + unmemoized `spaceTitleFor` O(P×N) — cliff at thousands of entries; swatchColor has no fallback for unknown names (transparent chip, invisible initial; 8 swatch tokens exist) and space-bearing names yield invalid CSS; `title.charAt(0)` breaks surrogate-pair-leading titles; stock `text-white` on swatch spans (same @theme-reset trap as Rail logo); "Active projects" heading applies no state filter; no HomePage empty state for 0 spaces/projects; views `order: null` sorts as 0; Sidebar.test navigation name overstates (space-row click untested), empty states untested; HomePage.test `getAllByText(...).length > 0` could pin `.toBe(2)`
- stores/shell (Task 17 reviews, minors, mostly plan-verbatim): if patchFrontmatter's revert `rescan()` itself rejects, the rejection propagates and the stale optimistic entry sticks (pre-existing Task 11 behavior; consider second toast "Couldn't refresh vault"); vaultStore.test's toast test leaves one toast in uiStore (beforeEach resets vaultStore only); navStore has no consecutive-duplicate dedupe or history cap; Rail container is a `div` (no `nav` landmark); shell uses arbitrary-value classes (`bg-[var(--n-0)]`) though the Task 2 `@theme` bridge registers named utilities; third copy of the Tauri check (`isTauri` in App.tsx vs `inTauri()` in vaultStore/ipc); Rail logo tile uses stock `text-white` — will break when the deferred `@theme` stock-palette reset lands; plan-doc note: Task 17 Step 12's "no last vault in mock" premise conflicts with Task 10's mock `getLastVault` always returning `/demo-vault` (chooser path covered by App test + Task 24 dual-path smoke)
