# M25–M28 execution handoff

**Living document.** Every phase that lands updates it in the same commit.
Written 2026-08-09 for whoever picks this branch up cold — including a future
context of the same agent after compaction.

---

## Where things actually stand

Branch `m22-m28-convergent-intelligence`, not pushed, no PR. **M21, M22, M23,
and M24 are DONE and committed** (through `e7cdb5e`). **M25 is DONE** — all
eight phases. **M26 is IN FLIGHT**: M26.1 (the entity resolver), M26.2 (basic
semantic retrieval + the semantic receipt), M26.3a–d (the anti-self-ancestry
walk, policy.v2 + the binding, the live proposal surface switched OFF by
default, the target binding), M26.3e (the e2e clock pin), and **M26.4a–g** (the
semantic disposition event, the settled window, the prompt + §92 fence, the
taint row + terminal batch, in-session retry as data, the ambient driver, and
the retry bound at the MCP boundary) are committed.

**M26.4 is DONE (a–j) and M26.5 is IN FLIGHT (a–b of e).**

**M26.4a–i are committed, and the spine RUNS.** `ingest::driver::tick` walks
scan → catch-up → deterministic pass → budget gate → one run → closure, and
`ingest::ambient` is the thread that calls it — **off by default**
(`ambient.ingest_enabled`, per vault). The three missing links are closed. See
"M26.4i — DONE" for the six decisions and the declared limits. M26.4j
(retiring the old distill lanes) is now UNBLOCKED. M26.5–M26.9 have not
started, nor have M27 and M28.

**The gate is fully green.** The e2e spec that used to fail on every tree was a
fixture shelf life, fixed in M26.3e by pinning the clock rather than bumping
the corpus. There are no known failures, so a red gate now means something you
did.

Read, in this order, before touching anything:

1. `AGENTS.md` — the house rules. The ones that bite in this milestone:
   policy-is-data (a rule written twice in Rust and TS is a review-blocking
   defect), two-records-two-destinies (telemetry never enters the vault
   ledger), store-layer never-throw is HUMAN-UI ONLY (proposal channels return
   typed results the caller must read), ratchets only tighten.
2. `docs/superpowers/plans/2026-08-07-cerebro-m25-metering-budgets.md` — the
   phase list and acceptance matrix.
3. `docs/superpowers/specs/2026-08-08-cerebro-m25-metering-budgets-design.md`
   — **the spec wins** on any disagreement with the plan.
4. `shared/policy/README.md` and `shared/runtime/README.md` — the judgment-call
   logs. Add to them; do not silently decide something they already decided.
5. This file's "Ground truth corrections" section — the plan cites several
   line numbers that are wrong, and one factual claim that is wrong.

### Gate commands (all must be green per phase)

```sh
cd src-tauri && cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test
pnpm lint && pnpm typecheck && pnpm format:check
pnpm test:run          # NEVER `pnpm test` — that is watch mode and never exits
pnpm test:coverage     # CI runs this; the branches floor is 80 and ratchets UP only
PORT=<free> pnpm e2e   # pick the port AFTER checking it is free — see below
```

**Check the e2e port is free first, every time.** `playwright.config.ts` sets
`reuseExistingServer: !CI`, so a port that is already listening is REUSED —
Playwright never notices it is a different checkout. Other worktrees' dev
servers now hold 5273 (`m29-mermaid`) and 5373 (`m30-multi-repo-workspace`),
and running against one of those produces a long, plausible, entirely
meaningless failure list: the specs load an app built from a branch that
predates M24.9, so every review/pipeline/knowledge spec fails on a rail
button that does not exist there. Thirteen "failures" in 6.9 minutes; the
same suite passes 47/47 in 53 seconds on a free port.

```sh
p=5473; lsof -nP -iTCP:$p -sTCP:LISTEN >/dev/null && echo "$p BUSY — pick another" || PORT=$p pnpm e2e
```

Conformance vectors regenerate with
`UPDATE_CONFORMANCE=1 cargo test --lib ledger::conformance`, then re-run
`pnpm test:run src/lib/epistemic`. Commit both sides together and say WHY in
the message. Never `--no-verify`.

One commit per phase: `type(scope): sentence (M25.n)`.

---

## Ground truth corrections to the plan

Verified against the tree; the plan's prose is right and several of its
citations are not.

| Plan says | Reality |
| --- | --- |
| usage discarded at `agent.rs:33/768` | `:33` is the `AgentEvent` enum (no member could carry usage). The real drops were the `translate` arms at `:871`, `:897-916`, `:933-960`. Fixed in M25.2. |
| mtime `behind` heuristic at `okf.ts:407` | It is `src/engine/okf.ts:428` (`entry.modifiedAt > at`) inside `commitOf`. **Two callers**: `src/engine/learn.ts:96` (the job feeder — this one dies) and `src/knowledge/KnowledgeCommit.tsx:52` (a shipped UI chip — this one must keep working). Deleting `commitOf` outright breaks the knowledge panel. |
| failed run forgotten at `useJobRunner.ts:405-409` | True, but the range straddles two paths: `:405-406` is the run-level failure (real), `:408-419` is a startup throw with a session-only escape hatch. |
| runtime DB has "one `operational_log` table" | It shipped at `user_version = 2` with `operational_log` AND `parked_promotions`. |
| the policy table is `policy.v1.json` | Since M26.3b the shipped table is **`policy.v2.json`** (`format: 2`). v1 is frozen and still compiled in as the registration gate's negative control. `POLICY_JSON` / `POLICY_PATH` / `POLICY_DIGEST_PATH` name the live one; the `POLICY_V1_*` consts name the frozen one. |
| the semantic receipt is `search_version: 2` | It is **3**. The design assumed M24 shipped v1, but M24.7 already bumped to 2 when it made the three deterministic legs real. Reusing 2 would leave an M24.7 receipt (`not_available`) and an M26.2 receipt (`completed`) claiming the same version, which is the one thing the version exists to prevent. |
| relations join entities | They join **BELIEFS**. `supersedes`/`refines`/`contradicts` are claims about claims, and the reducer resolves an endpoint in `beliefs` (`reduce.rs`'s explicit-relation basis walk). A traversal written against entity ids silently matches nothing — it cost a rewrite in M26.2. |
| runtime DB lives in "app-data" | It lives in `app_config_dir()` (`lib.rs:25-27`, armed at `:436-446`). **Do not move it** — that orphans non-rebuildable refusal history. |
| `vault_id` exists | It did not. M25.1 introduced it (see below). |
| lane order `filed…schema` | Correct — matches `src/engine/jobs.ts:27` and its `RANK` at `:298-306`. |

Other traps found by recon, still live:

- **`RUN_SEQ` restarts at 0 every launch** (`agent/mod.rs`) and is
  simultaneously the mcp-config filename, the `stop_agent` argument, and the
  event tag. It is NOT a durable key — M25.2 mints a separate 128-bit
  `run_id` for the database (`ledger::new_run_id`).
- **The e2e kill switch has 7 independent copies.** `cerebro.autoLearn=false`
  is inlined in `page.addInitScript` in the smoke, pipeline, agent,
  collections, inbox, knowledge, and review specs. Keep it working as an
  independent AND-gate; the plan says so explicitly.
- **`stop_all_agents` exists and is never called**; there is no shutdown hook
  and no vault-switch hook. `app_sessions`' clean-close stamp needs one.
- **The TS reducer throws on unknown kinds** (`src/lib/epistemic/reduce.ts`),
  and `mockIpc` reduces for the capture path — so a Rust-only event kind
  breaks the mock. Rust and TS land in the SAME commit, every time.
- **Coverage ratchets**: `vite.config.ts` branches floor is 80. UI work with
  error branches will trip it; write the tests with the code.
- **`independence.rs` refuses `human_confirmed`** on a now-false premise ("its
  validator ships with M24" — M24 shipped). M25.5 touches this file; fix or
  re-justify the refusal there.

---

## What M25.1 and M25.2 built (do not re-derive)

### M25.1 — `c76f677`

- `src-tauri/src/runtime/schema.rs` — the whole `user_version = 3` DDL as an
  executable contract: closed enums are `CHECK`s, counts are `>= 0`, hashes
  are shape-checked, times are checked for the `…Z` spelling. Two constraints
  are behaviour: an ambient run must name a vault, and a `claimed` item must
  carry a lease.
- `runtime/mod.rs` — migrations are a numbered list, each one `BEGIN
  IMMEDIATE` → DDL → **validation inside the transaction** → version stamp →
  commit. Failure rolls back, names the version, pauses ambient. Corruption is
  quarantined to `runtime.db.corrupt-<stamp>`, never overwritten; an
  `runtime.db.open` marker file distinguishes clean from unclean shutdown
  (`quick_check` vs `integrity_check`). `open_existing()` is the hot-path
  connect that refuses to migrate.
- `runtime/scope.rs` — **`vault_id` is derived from the canonicalized path AND
  recorded** in `vault_registry`. Derived so a deleted database re-derives the
  same ids and rebuilt rows rejoin their vault. Moving a vault folder mints a
  new `vault_id`; that is correct for the local questions it answers, and
  `store_uuid` carries portable identity.
- `runtime/normalize.rs` — the normalized snapshot. **No timestamp is in it,
  by construction**; that is the whole point (a `git checkout` rewrites every
  mtime and changes no content). `NORMALIZER_VERSION = "vault-entry-v1"`.
- `runtime/scheduler.rs` — row storage + the seven states. `put`,
  `counts_by_state`, `keys_in_state`, `get`, `move_state`.
- `runtime/import.rs` — the one-shot upgrade. Imports the three ledgers that
  were actually durable (`cerebro.learnAttempts` / `.skillRuns` /
  `.triggerRuns`) and HOLDS everything it cannot prove as `baseline_held`.
  Consults mtime exactly once, deliberately, in the one place the legacy
  evidence is keyed on it. Rows and the completion marker commit together.
- `runtime/recovery.rs` — the conservative recovery planner.
  `receipts_in_ledger()` is the **M25.3 seam** and returns empty; the planner
  it drives is complete and tested against injected receipts.
- `runtime/status.rs` — process-level `Ready | MigrationFailed | Recovering`.
  Has a `test_lock()` because it is global; any test that asserts on it must
  take the lock or it will pass alone and fail in the suite.

### M25.2 — `7f1dfea`

- `agent/usage.rs` — parses the four disjoint counts off recorded fixtures in
  `src-tauri/fixtures/cli-stream/`. Unknown wire fields → one
  `operational_log` row per run. `KNOWN_UNCOUNTED` is the deliberately-ignored
  list, kept separate so the log does not fill with the same two lines.
- `agent/meter.rs` — the reader thread tallies and writes straight to the
  runtime DB. **Usage never travels on the UI event channel.** Also holds the
  elapsed watchdog.
- `shared/runtime/budget-defaults.v1.json` + `.sha256` — compiled in,
  digest-pinned. Regenerate with
  `cargo test --lib runtime::budget::tests::write_defaults_digest -- --ignored`.
- `runtime/budget.rs` — immutable settings versions (every edit appends, even
  a revert), local-day windows via `chrono-tz` (honest 23/25-hour DST days, a
  skipped midnight starts at the first minute that exists), `budget_days` rows
  that COPY their ceilings, and the gate. **Degradation is one formula**: each
  lane's shed threshold is interpolated from its registry priority (lowest
  sheds at `warning_ppm`, highest at the ceiling), and the refusal names the
  dimension that is filling rather than an opaque "degraded".
- `runtime/dispatch.rs` — `claim()` is gate + reservation + scheduler claim +
  run row + singleton lease in ONE `BEGIN IMMEDIATE`; spawn after commit.
  `finalize()` is the second transaction. Missing usage is `unknown`, never
  zero, and pauses ambient. `recover_expired_leases()` is the crash path.
  Kill-point tests cover every boundary.

### Crash points now armed (for `testutil::run_crash_scenario`)

`runtime-db-migration-{n}-{begun,ddl,validated,committed}`,
`runtime-db-quarantined`, `runtime-import-{begun,rows-written,committed}`,
`runtime-recovery-{begun,committed}`,
`runtime-dispatch-{begun,claimed,committed}`,
`runtime-finalize-{begun,committed}`.

---

## M25 is COMPLETE — all eight phases committed

| Phase | Commit | What it added |
| --- | --- | --- |
| M25.1 | `c76f677` | scoped runtime DB, numbered transactional migrations, corruption quarantine, `vault_id`, the normalizer, the upgrade importer, the recovery planner |
| M25.2 | `7f1dfea` | usage parsing off recorded CLI fixtures, the metered reader thread, the versioned budget artifact, local-day windows, the gate, the one-transaction claim, the elapsed watchdog |
| M25.3 | `9f06ea1` | `ingest.assessed` + the closed route matrix, content-hash launch catch-up (the mtime feeder is gone), sessions with honest close precision, responsibility contracts |
| M25.4 | `f5ed327` | the four coverage kinds, seven uncollapsed dimensions, `CoverageAssessment` reshaped in place, the M24 policy rules updated |
| M25.5 | `e11e1b6` | the materiality prefilter, `independence-rules.v1.json`, the two positive independence producers |
| M25.6 | `d56e389` | three faces of failure — runtime health, source connection/health, ingestion failures — plus window-keyed quota backoff |
| M25.7 | `65bab37` | the control surface: pause, meter, lane toggles, Activity log, banners, held-work choices |
| M25.8 | this one | one commit per applied batch, and the simulated-day soak |

Gates at M25.8: **Rust 831 · TS 2319 (5 skipped) · e2e 47** — all green.

### Things a later phase will want to know

- **The lane shed ramp is real and visible.** The soak found it: `behind` is
  priority 3 of 7, so it sheds at 90% of the run ceiling (18 of 20), leaving
  the last runs for `filed`. That is degradation working, not a bug — but any
  test that assumes "20 runs happen" will be wrong. Assert `<= 20` and, if
  you care which lane, assert the ramp.
- **`Button` does not forward `data-testid`.** Locate buttons by accessible
  name (`getByRole('button', { name: … })`), which is what the review specs
  already do.
- **The process-global `status` needs its lock.** Any test that reads
  `status::ambient_allowed()` or calls `status::clear()` must take
  `status::test_lock()`, or it will pass alone and fail in the suite. This
  bit twice.
- **Foreign keys and CHECKs caught two real ordering bugs** (a claim written
  before its run row; an ambient run with no vault). When SQLite refuses
  something, read it as a design answer first.
- **Three conformance regenerations happened in M25**, each deliberate and
  explained in its commit: `ingest_receipts` (M25.3), the three
  `coverage_*` keys (M25.4), and one changed refusal detail (M25.5).
- **`KINDS_WITHOUT_VECTORS`** in `conformance.rs` names `proposal.reverted`
  as the one kind with no vector, and an exhaustiveness test proves the
  covered list plus the gap list is the whole vocabulary. Add to one or the
  other when you add a kind.

### What M25 deliberately did NOT wire

These are seams, not omissions, and each is a named function with tests:

1. **Nothing dispatches ambient work automatically.** `dispatch::claim` is
   complete and tested; no caller runs it on a timer. M26 turns that on, and
   the plan is explicit that it must not happen before then.
2. **`ingest.assessed` receipts are not yet emitted by the pipeline.** The
   body, reducer, route matrix, and TS twin all exist and are vector-proven;
   the producer that batches an Observation with its first receipt is M26's
   ingest pass. `recovery::receipts_in_ledger` already READS them.
3. **Coverage facts have no adapter producers yet.** The bodies, validation,
   reducer, and cache tables exist; `health::record_probe` writes the
   operational half. The `coverage.fact_recorded` emission belongs with the
   connector adapters (§61 spirit — connectors arrive as data).
4. **The blindness threshold computes its dimensions but does not append the
   gap.** `health::blindness_gap_dimensions` returns what a gap would affect;
   appending `coverage.assessed` + `coverage.gap` needs a ledger writer in
   the health path, which is M26's neighbourhood.
5. **`useBatchCheckpoint` exists and no caller uses it yet** — the apply path
   in `ReviewPage` still just reloads. Wiring it is a two-line change once
   `decideProposal` returns the batch id.

## M26 — in flight

`docs/superpowers/plans/2026-08-07-cerebro-m26-platform-agents.md` (9 phases)
and its spec dated 2026-08-08. **This is where LLM behaviour returns**, and
the plan is explicit that the flip is defensible only because M24 gates
writes, M25 meters spend, and the preventive anti-self-ancestry check runs at
apply. If any of those regress, flip nothing.

### M26.1 — DONE (`43a5072`)

`src-tauri/src/ingest/resolver.rs`. Five deterministic tiers ending in
`unresolved`, a closed tagged `Attempt` union, and `validate()` holding every
structural invariant the design states. Read its module doc before writing
M26.2 — the tier ORDER is load-bearing and the reasons are written down.

Not yet wired: nothing calls `resolve()`. It is a pure function over
`EpistemicState` plus an extractor-supplied `Candidate`, and the extractor is
M26.4's. Also not yet persisted — the `resolver_outcomes` runtime table the
spec names (design §653) is not in the schema; adding it is a
`user_version = 4` migration following the M25.1 pattern exactly.

### M26.3a — DONE (`b1a6010`)

`src-tauri/src/policy/ancestry.rs` — the preventive walk, with all ten
vectors the plan names (direct, transitive, old-revision, cycle, unrelated
control, opposing-role, risk-blindness, no-basis-op). Read its module doc:
it explains the three hop kinds and, at the end, exactly what is NOT wired
and why that ordering is the safe one.

### M26.3c — DONE (this commit) — **AGENTS CAN NOW MUTATE**

This is the commit the whole milestone was arguing toward, and the one to read
first if something goes wrong. The proposal tools are registered on the live
loopback MCP server, **behind a switch that defaults off**.

Three decisions were the owner's, taken after a written comparison:

1. **One tool per op**, `propose_<op>`, plus terminal `commit_proposals`. The
   prefix also resolves the `cache_source` collision — it is both a
   hand-written write tool and a policy op.
2. **`revert_proposal` is NOT agent-facing**, expressed as `agent_facing:
   false` in the artifact rather than a list in Rust. It is MEDIUM, MEDIUM
   auto-applies, and an agent-facing revert would silently undo an applied
   mutation — including one a human had just approved on a HIGH card, with no
   second card to notice. Every other op is agent-facing; the default is true,
   so the artifact says only where the answer is no.
3. **A real kill switch**: `AppConfig.agent_proposals_enabled`, default false.
   There was none before — `cerebro.autoLearn` is the distiller flag and
   `ambient.global_pause` is M25's ambient pause, and neither gates the
   catalog. **This is what M26.9 flips.** Registration is not activation.

Implementation notes a reviewer will want:

- **The names are GENERATED** from `table.agent_facing_ops()`. There is no
  second inventory — and `tool_policy` (the `--allowedTools` grant in
  `agent/mod.rs`) was a third one, hand-maintained with nothing asserting it
  matched the served catalog. It now derives from `mcp::tool_catalog(true)`
  and `the_grant_is_exactly_the_served_catalog` proves it.
- **The registration gate** (`mcp::registration_gate`) refuses to build ANY
  proposal tool unless `ancestry::table_binding` passes and `create_belief`
  still requires `candidate_receipt_current` and declares its four receipt
  codes. Against the frozen v1 table it refuses by naming the absent binding.
- **`run_id` rides the bearer token** (`mcp::run_id_of`). `commit_proposals`
  refuses members belonging to another run, and that check is only meaningful
  if a caller cannot name a run it does not hold the token for.
- **The receipt is minted server-side** inside the create path; a
  caller-supplied one is refused `candidate_receipt_caller_authored`.
- **`payload` is an open object in the inputSchema** on purpose. A
  hand-written JSON Schema per op would be a second copy of `ProposalOp`'s
  twenty closed variants, free to drift from the frozen union. serde plus
  `ProposalV1::validate` is the real validator.
- **Both absence assertions were INVERTED, not deleted** —
  `policy::submit::the_live_proposal_inventory_is_the_policy_inventory` and
  `src/engine/tools.test.ts`'s "offers exactly the proposal surface the policy
  artifact authorises". Each now proves the served set equals what the
  artifact authorises, in both directions, with synonyms and the human's own
  `record_decision`/`resolve_commit_set` still forbidden.

**A trap this phase found and closed:** `propose_organize` is a hand-written
tool that predates this namespace and is not a policy op. A bare
`strip_prefix("propose_")` routes it to the mutation boundary; it escaped only
because its match arm came first, and arm order is not a security property.
`proposal_op_of` asks the table instead.

**Still not wired after this phase:** nothing dispatches ambient work
(`dispatch::claim` still has no production caller), so this is an ATTENDED
surface only until M26.4/M26.9.

#### What a 27-agent adversarial review found, and what is left

Five attackers plus per-finding refutation. Eight claims survived; three were
fixed here, and **two of the survivors are why the switch must stay off.**

Fixed in this commit:

- **CRITICAL — `propose_append_observation` forged the ledger's actor.** Its
  payload is the server-canonical `ObservationRecorded` body, and
  `expand.rs`'s arm for it is the only one of twenty that passes the caller's
  `actor` through instead of stamping the run's. `reduce.rs` derives
  AUTHORITY from that actor, so an agent could mint a `TrustedHumanCapture`
  observation attributed to the human — LOW risk, auto-applied, no card. The
  predicate that should stop it, `trusted_observation_provenance`, is
  required by the row and evaluated NOWHERE; its declared owner
  `AgentObservationDraft` is dead code. **Fix:** `agent_facing: false` until
  the draft→recorded conversion exists.
- **Synthesized `intended_use`/`basis` defaults were the modal path** — the
  schema advertised only `[payload, targets, reason]`, so omission was
  normal, and the defaults understated stakes and asserted a cause nobody
  supplied. Both are required now.
- **The proposal path never met `knowledge::guard_agent_write`**, so an agent
  could patch `/fields/verified` — signing the review it exists to be checked
  by. Refused in `tool_propose`.

**M26.3d closed both of these.** The write-up below is kept as the record of
what was found and why the fix took the shape it did.

**CLOSED (M26.3d):**

1. **`targets` is not bound to `payload`.** CAS (`versions_current`) and both
   escalators (`target_has_attestation`, `lineage_fan_in`) read
   `proposal.targets`, while the mutation is expanded from `proposal.op`. An
   agent can declare a harmless target — a fresh id with
   `expected_version: null`, which reads as a creation — and mutate something
   else entirely, so **an attested Belief can be revised without becoming a
   human card.** `target_set_exact` only checks target CLASSES. The fix is a
   per-op extractor naming the ids an op touches, server-derived; there is no
   such function today, and inventing one per op in `mcp.rs` would be the
   second inventory this milestone forbids. It belongs in the schema layer
   beside `target_classes()`.
2. **`write_target` returns `None` for every proposal tool**, so `RunGrant`'s
   folder scope is never applied to them. Narrower than it sounds — the
   proposal surface writes through the ledger rather than to paths — but a
   scoped run is not scoped here, and the doc comment on `write_target`
   claims any future write "will be caught by
   `no_write_tool_escapes_the_scope_check`", which enumerates three hardcoded
   names and would not catch one.

### M26.3d — the target binding (this commit)

**The rule, in one sentence a reviewer can check:** for every `(class, id)` the
op's own expansion would ADVANCE, whose id the payload NAMES, `targets` must
contain that exact pair — otherwise `target_set_mismatch`.

- **Containment, not equality.** Extra declared targets stay legal: a target an
  op only reads keeps its version, and `commit.rs` is built to make a read
  legible as a read. Extra targets widen CAS and turn MORE escalators on, so
  they cost the caller and buy it nothing. Equality would also have broken
  seven shipped goldens that legitimately declare reads.
- **Payload-named, and this is the part three independent mappers all got
  wrong.** Relation ids are `sha256("cerebro-relation-v1\0" ++
  canonical_json([from,to,kind]))` and no tool hands a caller a way to compute
  one. Plain containment over the whole write set would have made
  `supersede_belief`, `split_belief`, and both merges **unproposable by any
  agent**. A server-derived id is also not an attack surface — it is a pure
  function of ids the payload names, which the rule does check.
- **The extractor already existed**, and the M26.3c handoff was wrong to say
  otherwise: `Expansion.write_targets` is exactly "the (class,id) pairs this
  plan advances". `write_targets_of` CALLS `expand` rather than re-matching on
  the twenty arms — a second match would be the second inventory, and the two
  copies would diverge precisely on the conditional arms
  (`merge_beliefs_exact` revises the survivor only when the basis differs) and
  the state-derived one (`revert_proposal` reads its steps from the stored
  plan). The cost is expanding twice per commit; the alternative is a
  predicate allowed to disagree with what actually gets written.
- **Home: `preconditions::check`**, under the predicate name the table already
  uses. Zero artifact churn — all 20 op rows already require `target_set_exact`
  and declare `target_set_mismatch`, which is already ledger-destined. Not the
  schema layer: `submit_proposal` returns on `validate()` failure BEFORE
  appending `proposal.submitted`, so a schema-home refusal is structurally
  incapable of producing a ledger record. Not the table stage: `ProposalFacts`
  carries no ids in either language, so teaching it ids means a TS twin
  including the relation hasher.
- Sorted `requires` puts `target_set_exact` before `versions_current`, so a
  card says "you did not name what you are changing" before "your version is
  stale" — and that order is the artifact's, not a branch order.
- **Companion fix:** `append_observation` and `cache_source` declared no write
  targets at all, so a binding rule would have exempted them by matching an
  empty set. Both now declare the Source, which the reducer really does bump.

`PREDICATE_OWNERS` moved from `M24.1 // the table's target-class stage` to
`M26.3 // classes: the table stage; ids: target_set_bound`.

Also closed: the scope tripwire `no_write_tool_escapes_the_scope_check` named
three tools and called itself total. It now partitions the WHOLE served
catalog into scope-checked and explicitly-exempt-with-a-reason, so a new tool
in neither half fails. The proposal surface is exempt by declaration: it writes
through the ledger, not to a path, and `RunGrant.scope` is a folder primitive
that cannot express "which Beliefs" — governing that is the policy table's job,
which it already does per op and per risk.

### M26.3e — the suite's shelf life (`<this commit>`)

**`e2e/pipeline.spec.ts` "augment: Home volunteers…" had been failing on any
tree, including clean ones before M26 started.** Not a regression and not a
flake — the suite had a shelf life and it expired on 2026-08-11.
`recentlyLearned` only offered concepts stamped within the last **14 days**,
demo-vault's newest unverified stamp is fixed at `2026-07-28T09:26:00Z`, and
the "Recently learned" card stopped rendering at all the morning the cutoff
crossed it.

Fixed by pinning the clock, not by bumping the corpus. Bumping stamps moves
the expiry a fortnight and leaves the class of failure in place; and per
AGENTS.md a demo-vault edit is a test change that churns assertions elsewhere.
Three parts:

- **`e2e/boot.ts` is new and is the only `boot` now.** There were six copies —
  five specs plus an inline one in `smoke` — and AGENTS.md told you to make a
  seventh by copying one. It exports `boot`, `readMockFile`, and `VAULT_TODAY
  = '2026-07-28T12:00:00Z'`, the day the corpus was written to be read on
  (knowledge stamps run 2026-07-19…2026-07-28; the work it describes is dated
  2026-07-29 onward). `page.clock.setFixedTime`, **not** `clock.install` —
  freezing what the clock READS without faking timers, so intervals,
  debounces, and Playwright's own waiting still run on real time.
- **`playwright.config.ts` pins `timezoneId: 'UTC'`.** The app reads LOCAL
  date parts (`todayIso`, `toIsoDate`), so a pinned instant is only a pinned
  calendar day if the zone is fixed too. Without this the same commit is a
  different day on a laptop than in CI.
- **`recentlyLearned(concepts, today: string)`** — it took a `Date`, and its
  single caller (`LearnedCard`) passed `todayIso()` to `listConcepts` and a
  second raw `new Date()` to this, inside one `useMemo`, with the raw one
  absent from the deps array. Two reads of the wall clock in one memo that
  disagree across local midnight. It now takes the same string, compares
  calendar dates as strings via `addDays(today, -days)`, and the fortnight no
  longer slides with the hour. Nine unit tests, where there had been none:
  boundary day in, day before out, hour-invariance, human-reviewed excluded,
  superseded excluded, unstamped skipped, unparseable stamp skipped (so junk
  cannot sort above ISO dates onto the one surface Home volunteers), newest
  first capped at three, and the caller's overrides.

Gate after: **e2e 47/47** (46s, `PORT=5573`), TS **2339 passed / 8 skipped**,
lint · typecheck · format clean. Check the port is FREE before running — a
busy one is silently reused and runs the suite against another worktree.

Do not set `agentProposalsEnabled` until the surface has been exercised
end to end. The new tripwire
`the_predicate_with_no_evaluator_guards_nothing_that_is_served` will also fire
the moment M27 makes `contradiction_edges` available without wiring
`open_contradictions_addressed` — five agent-facing ops require it today and
are excused only because its subject matter cannot exist yet.

### M26.4a — the semantic disposition (`<this commit>`)

The event M25's two unfinished prefilter branches have been waiting for.
`src-tauri/src/ledger/schema/semantic.rs` + `KIND_INGEST_SEMANTIC_ASSESSED`.

- **The outcome table is closed by construction.** `SemanticOutcome ×
  SemanticDisposition` is 3×3 and exactly 3 pair; `disposition()` returns the
  one legal partner and `validate` compares against it, so the other six do
  not deserialize. `six_of_the_nine_outcome_disposition_pairings_do_not_exist`
  walks the whole matrix and counts the survivors.
- **The ids are the rule.** `derive_m26_batch_key(store, receipts)` — a window
  IS its queued receipts, sorted here so scan order cannot mint a second key,
  and a window that gained an item is a DIFFERENT window. Then
  `derive_semantic_assessment_id(store, window)`, **deliberately not a
  function of the outcome**: a second run over a settled window mints the same
  id whatever it concluded, so the reducer refuses it. "At most one semantic
  run per settled window" is arithmetic now, not a note in a plan.
- **No registered-target version effect, and it is asserted in bytes.**
  `apply_ingest_semantic_assessed` creates and bumps nothing. In
  `conformance/semantic.json` the `versions` map has no `semantic_assessment`
  class at all, and the proposal a material outcome carries is still at
  version 1 — being mentioned by history is not a state change.
- **A seam M25.3 left open is closed.** `Route::M26Completed`/`FailedVisible`
  required an `m26_outcome_event_id` and nothing checked it; the old
  `ingest.json` vector named a fabricated id and applied. `m26_batch_key` is
  now on `IngestReceiptState` and `check_receipt_against_outcome` requires the
  event to exist, to be an outcome, to agree about blocked-vs-decided, to be
  on the same window, to have READ the receipt being superseded, and to have
  submitted every proposal the receipt claims.
- Ordering inside a batch is real: the outcome must be applied before the
  successors it closes, because there is nothing to check against otherwise.
  `conformance/semantic.json`'s second window commits outcome + successor
  under one marker and proves it.
- `explanation` is bounded at 2,000 bytes (`MAX_EXPLANATION_BYTES`, mirrored
  in TS as a **byte** count via `TextEncoder` — `.length` would disagree with
  Rust on emoji). It is the one field a model writes freely into an
  append-only file.
- TS parity: `OUTCOME_RULES` in `src/lib/epistemic/schema.ts` is the twin of
  the Rust methods, plus `OUTCOME_SCHEDULER_STATE`. While there, the TS side's
  duplicate `failed_visible carries no proposal refs` check was deleted — it
  was already `proposals: 'forbidden'` in `ROUTE_RULES`, and Rust never had
  the second copy.
- `ALL_KINDS` is now a slice, not `[&str; 31]`, and carries an honest note:
  it is hand-maintained, and a kind added to `decode_body` but omitted there
  is invisible to BOTH coverage tripwires. That is the residual gap.

Gate: Rust **909** · TS **2340 / 8 skipped** · clippy · fmt · lint ·
typecheck · format clean. All 23 existing vectors churned by one line
(`semantic_assessments: {}`); `ingest.json` also gained the real outcome event
its successor receipt had been faking.

### M26.4b — the settled window (`e49a0f1`)

`src-tauri/src/ingest/window.rs`. `plan(store_id, &[Assessed]) -> Plan`, where
`Plan::window` is an **Option** — so "at most one semantic run per settled
window" is the type, not a rule. `runs_needed()` is 0 or 1 and there is no
third answer.

- An all-deterministic window mints no key, no assessment id, and no run.
  Asserted directly, because a pass that spent a run to conclude "nothing to
  do" would pass every other test in the file.
- The partition is total over the six routes a FRESH assessment can carry, so
  neither M25 branch loses work.
- The window key comes from the residual alone: two windows whose
  deterministic halves differ are the same semantic window.
- Refuses three inputs rather than guessing — a repeated receipt (deduping
  would mean the caller thinks it queued two and the key says one), an item
  with no receipt, and a terminal `m26_completed`/`failed_visible` successor
  fed back in (a loop that spends a run each time round).
- `Route::scheduler_state()` ↔ `SchedulerState::parse` are now tied together,
  with a test over every route.

**Not in this phase:** `dispatch::claim` still has no caller. The planner
decides; spending is M26.4d.

### M26.4c — the prompt and §92 containment (`a6b5791`)

`src-tauri/src/ingest/taint.rs` + `src-tauri/src/ingest/prompt.rs`.

- **The fence is unforgeable by construction.** Source bytes are wrapped in
  `<<<cerebro-source:NONCE …>>>` where the nonce is
  `sha256_first128("cerebro-source-fence-v1" | batch_key | item_id |
  content)`. To close its own fence a document must contain the hash of
  itself containing that hash. Derived rather than random so it is testable —
  `embedding_the_real_nonce_would_change_it` takes the nonce a document would
  have needed, writes it in, and shows it move.
- **The heuristic is modest on purpose**, and the module says so twice. It
  cannot make source text safe; a clean result is not "checked". There is a
  test named `the_heuristic_misses_and_that_is_documented_rather_than_hidden`.
  `CLASSIFIER_VERSION = "taint-v1"` is stamped on every assessment so a later
  reader can tell "v1 saw nothing" from "v2 never ran".
- Matching is **two modes**: `Match::Prose` against a punctuation-collapsed
  haystack (so `please-ignore-previous-instructions` does not get a free
  pass), `Match::Literal` against raw (because `<|im_start|>` and `mcp__` ARE
  their punctuation). `every_prose_pattern_can_actually_match` refuses a
  prose pattern that carries punctuation — it would be dead and read as
  coverage.
- **A flagged item is annotated and still included.** Dropping it would let
  any document remove itself from the base by looking suspicious.
- Counterevidence renders even when empty, in words: an absent section and an
  empty one look identical to a model and mean opposite things.
- `suspected()` is DERIVED from the signals, never stored beside them — a row
  saying "suspected" with no reason is an accusation nobody can check.

**Not in this phase:** the assessments are returned by `render()` and have no
writer yet. They belong in `runtime.db` keyed to the Observation event id
(operational destiny — they never touch the closed M22 body). That table is
M26.4d, and it needs a `user_version = 4` step plus an `EXPECTED_V4_TABLES`
entry.

### M26.4d — the taint row and the terminal batch (`fcf5ac6`)

`runtime.db` **schema v4** (`source_taint_assessments`, `USER_VERSION = 4`,
`validate_v4`) + `src-tauri/src/runtime/taint.rs` +
`src-tauri/src/ingest/outcome.rs`.

- The taint row is **operational**, and the placement is the argument: a
  classifier that wrote into the vault would make epistemic history a function
  of which pattern list was running, and re-running a newer one would rewrite
  the past. Keyed to the immutable Observation event id.
- `classifier_version` is part of the PRIMARY KEY. "v1 saw nothing" and "v2
  was never run" are different facts.
- Same key + different answer = **refused**, not overwritten. The assessment
  is a function of content that cannot change, so a disagreement means the
  patterns moved without the version moving.
- A CLEAN assessment is recorded, not skipped: no row and a row saying
  "nothing found" are different facts, and only one proves the classifier ran.
- `outcome::close()` builds the whole terminal batch and **refuses if the
  items do not exactly cover the window** — a queued receipt whose successor
  was never written is work the scheduler holds forever, already paid for.
- Proposals stay on the OUTCOME; successors carry none. The run proposed over
  the window and never said which item produced which proposal.
- `Closure::outcome_event_id` is **preallocated and derived** (the writer's
  `write_frame` takes an optional event id — `ledger/writer.rs:292`), so
  rebuilding a closure after an uncertain commit lands on the same event
  rather than a second one.

### M26.4e — in-session retry, as data (`6f17d1d`)

`policy.v2.json` gained `in_session_retry { max_attempts: 3,
retryable_rejections: [9 codes] }`; `artifact_version` 5 → 6, digest
regenerated. Both loaders read it; `PolicyTable::retry_verdict` /
`retryVerdict()` return **three** values, because "the table says no" and "you
have used your attempts" are different sentences.

The line: retryable = the STATE OF THE REQUEST moved (stale version, stale
receipt, CAS race, malformed message). Not retryable = the SUBSTANCE of the
change was refused. `human_rejected` is refused BY NAME in both loaders.
Growing the list needs the argument made in review.

### M26.4f — the ambient driver (`4105aca`), plus a fix to M26.4d (`9283259`)

**Owner decision: Rust.** `src-tauri/src/ingest/pass.rs` — `run_once()` is
`dispatch::claim`'s first production caller. Plan the window, claim it, spend
one run, close it, finalize.

- **The spawn is behind the `Runner` trait**; everything else is
  deterministic. The failure paths ARE the point, so the tests drive a runner
  that dies, one that succeeds, and one that must never be reached — against
  the real `LedgerWriter` and the real `reduce`, with each item's Observation
  and queued receipt committed as a batch first (a closure naming a receipt
  the reducer has never seen is refused, and that is worth seeing in a test).
- **Ledger first, runtime second.** They cannot share a transaction. The
  ledger is portable truth and the runtime DB is rebuildable from it, so a
  crash between them leaves terminal receipts recovery reads through
  `Route::scheduler_state`, and the lease expires into
  `abandoned_usage_unknown`, which pauses spending. The reverse order consumes
  an item with nothing saying why.
- **Spent-and-undescribable is HELD, never requeued.** Requeuing is an
  automatic second spend on work already paid for.
- The taint assessment is written BEFORE the run. One that only existed for
  successful runs would be missing for exactly the windows worth looking at —
  the test that proves it is the one where the runner dies.

**`9283259` corrected a real defect in M26.4d that every unit test passed.**
`append_batch` mints its own batch id and member event ids. The derived
outcome event id was not a member ref, so nothing substituted it, and every
successor receipt would have named an event nothing ever wrote — refused at
commit, in production. The fix uses `writer::member_ref(0)` (the symbolic
same-batch reference the writer substitutes, and what the operation digest
hashes) plus an operation key so a retry after an uncertain commit REPLAYS.
Two round-trip tests now live in `reduce.rs`, where the real writer is.

### M26.4g — the retry bound at the boundary (`be9466a`)

`tool_propose` counts a run's attempts per piece of work and refuses a
resubmission the table forbids, BEFORE evaluating it. Three typed refusals:
`retry_not_permitted`, `retry_exhausted`, `retry_unchanged`.

- The **work key is the op and its TARGETS, not the payload.** A
  `stale_target_version` retry refetches and resubmits with a new expected
  version; a key that moved with the payload would hand every retry a fresh
  allowance and the bound would count to one forever.
- `retry_unchanged` compares a digest over op + targets + declared risk +
  intended use + basis + reason. Byte-for-byte the same proposal back again
  is the loop the prompt forbids, made structural.
- The counter is process-global (a run is a bearer token, not a socket) and
  bounded by `RUN_TOKEN_WINDOW`: `push_run_token` calls `forget_attempts` for
  every grant it evicts.

### M26.4h — the window report and the CLI runner (`c0eae2a`)

Two pieces: `report_window_outcome` (the MCP tool a run reports through) and
`src-tauri/src/ingest/cli.rs`'s `CliRunner`.

- **The proposal list is the server's.** The tool takes the judgment only the
  run can make and nothing else; `proposal_ids` comes from what this server
  accepted from this run. A `material` report with no accepted proposals is
  refused — one of the two statements is wrong and the run can say which.
- **Silence is BLOCKED, never a guessed `non_material`.** A run that ends
  without reporting told us nothing, and the difference between a window
  nobody assessed and one assessed as quiet is the whole of §17. The spend is
  still recorded; missing usage stays unknown rather than becoming zero.
- The window is opened under the **MCP run id** (derived from the bearer
  token), not the dispatch lease's id, which the server never sees. Closed on
  every path including a failed spawn.
- Two parity tripwires fired and both were right: `policy::submit`'s served
  count went 12 → 13 (the tool is NOT gated by the M26.9 switch — a run must
  be able to say a window concluded nothing whether or not it could have
  proposed anything), and the TS catalog scrape wanted it in `ALL_TOOLS`. It
  lives in a new `UNPICKABLE_TOOLS` list rather than a toolset, because it is
  refused unless the driver opened a window and a checkbox for it would do
  nothing for any agent a person can build.

### M26.4i — DONE. The spine runs.

Eight commits, `3ccc4f3`…`07e560b`. The three missing links are closed and the
tick exists:

```text
scan → catch-up → deterministic pass → [budget gate] → one run → closure
```

`ingest::driver::tick` is the whole of it, and `ingest::ambient` is the thread
that calls it. **The switch defaults OFF** (`ambient.ingest_enabled`, per
vault; `ambient_ingest_enabled` / `set_ambient_ingest` flip it; no UI surfaces
it yet, which is M26.4j's or later). Wiring the spine was this milestone's
job; deciding an app may spawn CLI runs against somebody's subscription
unasked is not, and a default that spends money is not a default.

What landed, module by module:

| Module | What it is |
| --- | --- |
| `ingest/source.rs` | the vault as two Sources, split by provenance |
| `ingest/context.rs` | the prefilter's `Context`, with four `false`s each naming its absent producer |
| `ingest/assess.rs` | one item → one Observation + one receipt (the producer `ingest.assessed` never had) |
| `ingest/deterministic.rs` | the whole pass as one batch and one transaction |
| `ingest/retrieve.rs` | candidates split by a live `contradicts` edge |
| `ingest/driver.rs` | the tick, with the first-open gate |
| `ingest/spawn.rs` | the live `Runner` — mint a bearer, spawn, wait |
| `ingest/ambient.rs` | the supervisor thread and its switch |

**Six decisions worth not re-deriving.**

1. **The owner's notes are the OWNER's source, not a builtin.** The first
   draft registered `vault-files-v1` as a `SourceRegistration::Builtin`
   carrying `human_assertion`; the schema refuses that outright — authority is
   provenance derived from WHO, never a label on a service id. It now
   delegates to `capture::resolve_registration("human:owner")`, so a note
   typed in the app and the same note typed in an external editor are ONE
   source. Two identities for one person would make a single claim look like
   two, which is manufactured corroboration. `knowledge/` stays a `builtin`
   with `content_only`. Both are irreversible: `derive_item_id` and
   `derive_receipt_id` bake the source id into every identity.
   `capture::resolve_registration` and `source::resolve` both take an ORDINAL
   now — a first launch over a vault with a `knowledge/` bundle registers two
   sources in one batch, and two members claiming ordinal 0 would have one
   source's Observations pinning the other's registration.
2. **Exactly ONE Observation per receipt, structurally.**
   `IngestAssessed::validate_refs` wants `observation_event_ids` sorted and
   duplicate-free; `append_batch` mints random member ids and substitutes
   same-batch refs BEFORE validating. Two refs would pass about half the time,
   non-deterministically.
3. **A successor's dimensions are governed by its outcome.** THE defect this
   phase found: a `material_candidate` receipt could never be closed. The
   schema required a candidate to name a dimension and `outcome::close` clears
   the successor's by design — and with no deterministic mapper in this build
   EVERY material candidate queues, so that was the common case. Fixed in
   Rust, TS, and a new conformance vector; the two fixtures that could have
   caught it both happened to use `needs_semantic_judgment`.
4. **Counterevidence names its test.** `Candidate.disconfirming` became
   `Standing::{Uncontested, Contested}`, and `Contested` means "a live
   `contradicts` edge touches this belief", not "disagrees with this change" —
   the change has not been interpreted yet. The empty section reports which
   edge was walked. All-false was the one wrong answer available, because the
   prompt would then have asserted an absence nobody established.
5. **`agent::stream` has a completion signal.** `Tally::outcome` already
   computed `(RunOutcome, Option<Usage>)` and `meter::finish` discarded it.
   The receiver blocks on `recv()`, never `recv_timeout` — the elapsed
   watchdog bounds the run and sets the `aborted` flag the send reports, so a
   caller that timed out on its own would report an outcome the run never had
   and leave a live child behind. `Mode::Supervised` stops the meter
   finalizing: the ROUTE decides where items land, not the CLI's exit status.
6. **The residual is read from the SCHEDULER, not from the tick's own plan.**
   An item queued while the budget gate was shut stays `pending`, and the next
   catch-up correctly leaves it alone. A driver that only ran what it just
   planned would strand every item that ever waited a tick for budget. An item
   edited between assessment and run is skipped — the receipt describes exact
   bytes.

**Two smaller calls, in code:**

- `catchup::Queued` carries whether the ARTIFACT moved or only the normalizer
  did. A receipt claiming a file changed when only the rules did is a wrong
  answer in a portable log.
- `catchup::scan` returns what it could not read instead of `continue`-ing.
  The driver records an `ingestion_failures` row per name.

**Deadlocks avoided, both real and both structural.** `ingest::commit` exists
because `shadow::with_writer` holds a process-global mutex the MCP thread also
takes; `ingest::ambient` opens its OWN sqlite connection for the same reason
(`sink::with_sink` is a process-global mutex and `mcp.rs:857` takes it). In
both cases the rule is: never hold a process-global lock across a blocking CLI
run. Any future code that spawns a run must be read against that rule.

**Declared limits of this milestone** — each one is a passing test that will
FAIL when the limit stops being true, which is the correct behaviour:

- prose-only edits close as `non_material` (`normalize::snapshot` excludes the
  body by choice);
- `corroborates_existing_value`, `same_lineage`,
  `deterministic_proposal_available` and `residual_ambiguity` are all `false`
  with no producer, so every material change costs a run;
- `resolutions` is empty — M26.1's resolver runs per subject and an ingest
  window has no subject until the run finds one;
- `catchup::Plan.departed` is computed and not acted on: `item_key` IS the
  path, so a rename reads as a departure plus a new item, and closing departed
  rows would retire the history of every file anybody moved.

### M26.4j — DONE (`921787d`). The distillation lanes are gone.

`engine/learn.ts` is deleted, and with it `filed`, `behind`, `isLearnable`,
`unlearnableFiled`, and the persisted `filedForLearning` ledger.

**What survives, and why** — do not delete these thinking M26.4j missed them:

- `commitOf` and `KnowledgeCommit.tsx`. The panel answers "is what the base
  learned current with this note", which is not a queue.
- The manual **Learn from this** button. A person asking is attended, and was
  never the ambient lane.
- `scheduled` / `agent` (M13/M17/M18), `refresh` (cached sources), and the
  `stale` / `schema` concept-recheck lanes. **The rechecks are M26.6's** to
  replace; deleting them with M26.4j would have left a gap nothing fills.

**Filing is still the trigger, by a better mechanism.** Organizing a capture
WRITES the note; changed bytes are what the ingest tick watches. The old
renderer-side list could only ever see work the UI remembered to record, so a
note edited in an external editor was never re-read.

**The "Queued to be read" chip reads the durable scheduler** through a new
`ingest_item_state` IPC. The mock returns `null` for every vault — the same
answer the real backend gives when ambient ingest has never run — because
simulating a scheduler in `mockIpc` would be a second implementation of a Rust
table and would let a browser test claim a queue state no database ever held.
The `grow` e2e spec therefore asserts the WRITE, not the chip.

### M26.5 — DONE. All five sub-phases landed.

| Sub-phase | State |
| --- | --- |
| **M26.5a** `assembly/manifest.rs` | **DONE** (`682bd64`) — `QueryIntendedUse`, `WorkingMemoryManifest`, `ManifestItem`, `IntentRecord`, `Counterevidence`, `Limits`/`Actual`, 19 tests |
| **M26.5b** `assembly/answer.rs` | **DONE** (`d9a890d`) — the nine parts, ten dimensions, `EvidenceSufficiency`, `DiscoveryPlan`, cross-validation against a manifest, 16 tests |
| **M26.5c** the assembler | **DONE** (`a04ef98`) — `assemble.rs` + `corpus.rs`, 24 tests |
| **M26.5d** runtime tables | **DONE** (`e42299b`) — `SCHEMA_V5`, `store.rs`, 12 tests |
| **M26.5e** the attended run | **DONE** (`6ad6b00`, `20bae31`) — `prompt.rs`, `submit_answer`, `ask.rs`, `live.rs`, `ask_question`, mock + parity |

**What M26.5a/b decided, so nobody re-derives it:**

- `QueryIntendedUse::intended_use()` returns the M24 value ITSELF, so the
  stopping-rule lookup and the manifest cannot read two different things. The
  answer's use is compared byte-for-byte against the manifest's — a model
  cannot weaken kind, stakes, or predicate class between retrieval and
  synthesis.
- `attempted` is not spellable. The test asserts the deserializer refuses it.
- `exhausted` costs a recorded attempt, and counterevidence may only name
  attempts an intent actually recorded.
- `actual.source_count` is the distinct union across items AND attempts.
- `current_answer.basis_refs` is the EXACT union of what `basis` cites —
  neither superset nor subset.
- `SubjectRef::None` is refused in an answer even though M22 allows it.
- `DiscoveryPlan::mint` derives `plan_id` from the drafts and THEN numbers the
  steps; an authored `step_id` is refused.
- `validate()` (shape) is split from `validate_against(&manifest)`
  (resolution) because they are different claims and only one needs the
  assembly present.

**What M26.5c/d/e decided:**

- **Counterevidence is never trimmed.** Admission runs contradiction-first;
  if a contradicting item will not fit, `Refusal::CapConflict` and there is NO
  manifest. `blocked(cap_conflict)` on an intent record is the softer case —
  some OTHER intent lost everything. The contradiction intent can therefore
  never reach it, and a test asserts that invariant.
- **The source cap bounds the SEARCH.** An attempt records the sources its
  candidates came from whether or not they were admitted, so admitting fewer
  items cannot lower the count. A search wider than the cap is refused rather
  than narrowed: un-looking is not a thing an honest record can do.
- **Four refusals, each because its absence would be a lie**: `CapConflict`,
  `RetrievalUnavailable` (the retriever could not name what it missed, so
  counterevidence cannot honestly be included/exhausted/blocked),
  `Incoherent` (something selected but undescribable — a silent skip could
  turn found counterevidence into a reported `exhausted`), and `Invalid`.
- **The retriever is injected** so the blocked branches are real code a test
  drives; `expansion-v1` cannot fail.
- `corpus.rs` renders the CLAIM (predicate + value), never `extracted_text`.
  No raw source text reaches a persisted manifest. The bytes it renders are
  the bytes the manifest counts and the bytes the prompt prints.
- Beliefs carry no scope and no valid time in this tree; inventing one from
  their basis would be a claim the base never made. Scope-neighbour therefore
  has two legs: a conflicting slice (assertions) and one relation hop out
  (beliefs).
- `SCHEMA_V5` puts the discovery lifecycle in CHECK constraints, not only in
  Rust: `completed`/`failed` require a start, `dismissed` may skip it,
  `terminal_at` is set on exactly the three terminal states. A test drives raw
  INSERTs around `advance` to prove the DDL refuses each on its own.
- The same `assembly_id` with different bytes is REFUSED, never upserted.
- **The prompt's answer example is serialized from the type**, so a renamed
  field is renamed in the prompt in the same commit. It is a valid skeleton
  with placeholder refs, so submitting it unchanged is refused.
- `submit_answer` holds the manifest SERVER-SIDE — a run cannot name the
  manifest it is checked against.
- The receipt is recorded BEFORE the run starts, so a crash still leaves a
  record of what the base would have shown.
- The browser mock REFUSES rather than inventing an answer: a mock answer
  would carry a mock manifest whose every ref cited evidence that never
  existed.

**Two defects an adversarial review found in M26.5c/d, both fixed in
`6ad6b00` with the reviewers' reproductions as tests:**

- `assembly_id` omitted `request.aliases`, which change what the search
  reaches — two different manifests shared one id, and a reader fetching it
  could be handed the search that reported `exhausted` about a disagreement
  the other one found.
- `belief_context` named tombstoned beliefs and picked among candidates by
  hex id order.

**Still owed by M26.5, from the spec (unchanged — these are the two the
sub-phases documented but did not enforce):**

- The `coverage_dimension` basis ref must RESOLVE to a current,
  store/subject/scope-compatible M25 assessment and its exact named
  dimension; `DimensionAssessment::coverage_refs()` exposes the pairs and
  nothing checks them yet. The `as_of` = minimum-of-referenced rule is
  documented in the module header and is likewise unenforced until there is a
  coverage lookup to enforce it against.
- No UI surface calls `ask_question` yet. The command, the mock and the
  parity test exist; the panel that asks is not part of M26.5's scope.

### M26.6 — DONE. The pass runs.

| Sub-phase | State |
| --- | --- |
| **M26.6a** `maintain/candidates.rs` | **DONE** (`1da6dfb`) — exact-merge, compress and attention finders, 11 tests |
| **M26.6b** `pass.rs` + `prompt.rs` + `SCHEMA_V6` | **DONE** (`99b6b6d`) — content-keyed findings ledger, prompt, tick, silence regression, 11 tests |
| **M26.6c** `schedule.rs` + `live.rs` + the call site | **DONE** (`058e92a`) — ambient lease, live runner, wired into `ingest::ambient::tick_once`, 8 tests |

**Decisions, so nobody re-derives them:**

- **The pass operates on the belief base, not on `knowledge/` concepts.**
  Rust has no concept-staleness model and building one would duplicate
  `src/engine/okf.ts`. The `stale`/`schema` lanes in `jobs.ts` are STILL LIVE
  and still renderer-derived. Moving that ATTEMPT LEDGER to durable state is
  worth doing; moving the DERIVATION is not. **This is the one thing M26.6
  did not do that its plan line mentions.**
- **No clock is read anywhere in the finders**, and a test greps the module's
  own source to keep it that way. Compress is therefore "superseded, and
  nothing live points at it", never "superseded a long time ago".
- **Corroboration is not a duplicate.** Identical content resting on two
  DIFFERENT sources is not surfaced — merging it collapses two pieces of
  evidence into one.
- **The finding key is its content.** Said once; when the base moves the key
  moves and it returns by itself. That is the whole suppression mechanism and
  it is deliberately not a clock.
- **Findings are recorded AFTER the run**, so a failed or deferred pass
  leaves them unsaid rather than silencing something nobody heard.
- The prompt prints **no belief prose** — ids, source ids, and a closed signal
  vocabulary only.
- The silence regression is asserted against the **policy table**: no
  maintenance transition is reachable from a silence cause.
- **The findings are computed BEFORE the lease is claimed** — a quiet base
  claims no lease at all, so it cannot block an ingest tick that had work.
- Maintenance rides the SAME `ambient.ingest_enabled` switch, after ingest.
  It has no default of its own on purpose.

**Two defects caught while wiring, both now tests:**

- A reservation over `max_ambient_run_tokens` is refused by the gate EVERY
  time — a pass that can never run, looking exactly like a base with nothing
  to say. `the_reservation_fits_the_defaults_it_will_be_gated_against` is the
  tripwire.
- `Runner::run` takes the lease's `run_id`. The runner is built before the
  lease is claimed, so a runner holding its own id held the wrong one and the
  meter would book real tokens against a row nobody owns.

### M26.2 — DONE (`0ebbfcb`)### M26.2 — DONE (`0ebbfcb`)

`src-tauri/src/retrieval/` (`expansion-v1`) plus the semantic-carrying
receipt. **The user chose the deterministic option over local embeddings**
after a written comparison; the reasoning and its condition are below, and the
seam is built so the other choice is a `retriever_version` bump rather than a
rewrite.

- **"Semantic" here means over the knowledge GRAPH, not a vector space.** Two
  expansion paths: entities the proposed prose NAMES (matching the corpus's
  registered aliases against the query's text — the opposite direction from
  BM25, which is why one finds what the other cannot), and Beliefs one
  explicit relation hop from what the base already believes about the subject.
  `contradicts` is that second path's point: it is literally the accessible-
  counterevidence edge the assembly contract requires be looked down.
- **The boundary is asserted, not discovered.**
  `a_pure_paraphrase_is_not_recalled_and_that_is_the_documented_limit` proves
  `expansion-v1` returns NOTHING for a paraphrase with no shared entity,
  alias, or relation. Distributional similarity needs a model or a corpus far
  larger than demo-vault's 133 notes / ~9k words, where co-occurrence is
  noise. M26.5 inherits that gap as a committed fact; if a later retriever
  closes it, that test is the one that should fail and be rewritten
  deliberately.
- **No new index table, and that is the stronger reading of the requirement.**
  Retrieval is a pure function of `EpistemicState`, which the disposable
  SQLite index already rebuilds — so "app-data, never the vault, and a rebuild
  reproduces the same candidates" holds by construction instead of via a
  second cache that could disagree with the first. Purity is also a HARD
  requirement, not a preference: `preconditions::check` re-mints the receipt
  to validate it, so a retriever with hidden state would manufacture staleness
  out of nothing.
- **One hop, deliberately.** Two hops on a well-linked vault reaches most of
  it, which would make the semantic leg evidence of nothing — and every
  candidate costs the proposer a disposition to justify.
- **The receipt's semantic leg is `completed` with `retriever_version`,
  `index_head`, and `query_fingerprint`**, present exactly when completed.
  `attempted` stays spellable and is REFUSED: it is the shape a caller reaches
  for to claim credit for a search that did not finish, so the vocabulary
  keeps the word in order to have somewhere to say no.
- **The fingerprint closes a real hole.** The deterministic legs record their
  own queries, but the semantic leg expands over the proposed CONTENT and
  content is not stored on the receipt — so without it a caller could mint
  against innocuous prose and attach the receipt to a proposal saying
  something else. It caught a real fixture in `commit.rs` doing exactly that.
- **A semantic-only hit defaults to `distinct`, not `update`.** A
  deterministic hit means the identity is already here; a semantic hit means
  expansion reached something NEARBY, and the server is not entitled to say
  "revise that" about a neighbour. The reason string says which it was.
- `semantic_search_unavailable` is registered in a new `mint_rejections`
  registry (a fourth category — it fails before a proposal exists) with
  destiny `operational`. `expansion-v1` cannot fail, so the refusal is
  exercised through the injected-retriever seam (`candidates::mint_with`) —
  the only way to know the path works before a retriever that CAN fail is
  swapped in.

### M26.3b — DONE (`7b0f7e6`)

`shared/policy/policy.v2.json` (`format: 2`, `artifact_version: 2`) is now THE
table. `policy.v1.json` stays in the tree, frozen, and is still compiled in
and still parsed — read the judgment-call log in `shared/policy/README.md`
before touching either.

What landed, and the calls made along the way:

- **`no_self_ancestry` binds to exactly two ops**, `create_belief` and
  `update_belief` — the only `ProposalOp` variants whose payload carries a
  `BeliefBasis`, which is the only thing the walk can refuse. `split_belief`
  and `merge_beliefs_exact` redistribute evidence a Belief already rests on;
  binding them would read as broader protection and provide none.
  `ancestry::basis_target` is now an **exhaustive match with no wildcard
  arm**, so an op variant added later that does carry a basis cannot reach
  the table without the compiler asking first.
- **Where the gate runs is DATA**: a new top-level `preventive_ancestry`
  block, shaped like `contradiction_addressing`
  (`{required_for_ops, predicate, rejection}`). A list hand-kept inside the
  registration gate would be the second inventory the parity rules forbid.
  Load proves the three facts agree **in both directions** — every listed op
  requires the predicate and declares the rejection, and no unlisted op
  requires it — so "is the gate bound?" has one answer.
- **Format is a SET now**: `SUPPORTED_FORMATS = [1, 2]`, `FORMAT = 2`, both
  sides. Format 2 *without* the block fails the load; an unknown format still
  fails. v1 is kept because it is the negative control for M26.3c:
  `ancestry::table_binding(&v1)` must fail on the **absent binding**, not on
  an unknown code, and a hand-written stub could not prove that.
- **`unbound_rejections` may now be empty, and is.** `self_ancestry` was its
  only member. Both loaders grew a `check_possibly_empty_list`; demanding a
  member would mean keeping a fake reservation alive for a validator.
- `PREDICATE_OWNERS` gained `("no_self_ancestry", Some("M26.3"))` and
  `preconditions::check` dispatches to `ancestry::no_self_ancestry` off the
  op's `requires` list like every other predicate.
- Three goldens (`evidence-derived-from-the-belief-it-would-support-is-
  refused`, `a-restatement-of-the-bases-own-output-is-refused-too`, and the
  CONTROL `evidence-derived-from-another-belief-lets-the-verdict-through`).
  They declare an `ancestry` support graph and are therefore `rust_only`, the
  same declaration CAS fixtures make. **That is not a gap**: the TS runner
  skips only the verdict replay, and still asserts the artifact half over
  those files — that the op declares the code possible and the code declares
  a destiny. The walk's own five vectors live in `ancestry.rs`.

**M26.3c — register the live proposal surface — is what remains of M26.3, and
it is blocked on M26.2.** The registration tripwire needs BOTH the ancestry
binding (done: call `ancestry::table_binding`, which already refuses against
v1) and M26.2's semantic-receipt-v2 validator/goldens active. When you get
there, the parity assertion proving the tools are ABSENT **must be INVERTED,
not deleted** — that assertion is how anyone knows agents are off. It lives in
two places, and both read the same `tool_catalog()`:
`src/engine/tools.test.ts:39` (`serves no proposal tool`) and
`policy::submit`'s `no_proposal_tool_is_registered`.

### M26.2 and M26.4–M26.9 — the shape of what remains

| Phase | What it is | The trap |
| --- | --- | --- |
| M26.2 | semantic retrieval in app-data (query expansion over aliases/relations, local embeddings only if needed) | embeddings live in app-data, NEVER in the vault; the index is rebuildable |
| M26.3c | register the M24 proposal tools on the live + mock MCP servers | **the parity assertion that proves the tools are ABSENT has to be inverted, not deleted.** The gate landed BEFORE registration (M26.3a/b); the tripwire must require the ancestry binding AND receipt-v2 |
| M26.4 | DONE (a–j) | — |
| M26.5 | query-time assembly, attended-only — **a/b done, c/d/e to go** | two outputs never merged: retrieval adequacy and evidence sufficiency are separate structured values |
| M26.6 | maintenance pass | — |
| M26.7 | Source Monitor + attention primitives + governance metrics | the Source Monitor is a deterministic fetcher and must NOT be named Pattern Scout (amendment 6) |
| M26.8 | convergence synthesis | — |
| M26.9 | the flip + eval fixtures | "on by default" = deterministic always, LLM within M25's budgets |

The five M25 seams listed above are M26's inputs: `dispatch::claim` needs a
caller (M26.4/M26.9), receipts need a producer (M26.4), coverage facts need
adapter producers (M26.7), the blindness threshold needs a ledger writer
(M26.7), and `useBatchCheckpoint` needs the apply path to hand it a batch id.

## Remaining phases, with the decisions already made

### (M25.4–M25.8 are DONE — the notes below are kept as the record of what was decided)

### M25.4 — the coverage model
Five kinds are specified; `ingest.assessed` landed in M25.3, so M25.4 adds
`coverage.fact_recorded`, `coverage.assessed`, `coverage.gap`,
`coverage.restored`. **`reduce.rs`'s `CoverageAssessment` changes IN PLACE**
(no producer has ever emitted one, so there is nothing to migrate) from four
match fields to seven typed dimensions each carrying
`{state, basis_event_ids, as_of}`. That breaks, and must be updated together:
`policy/coverage.rs` (`match_field`, the high-stakes rule at `:266-306`, the
absence rule at `:409-469`, and the four test builders), plus
`policy.v1.json`'s `absence.required_coverage_dimensions` (currently **5**
names, the spec wants all 7 checked per-dimension) and therefore the policy
digest and the goldens on both sides. Expect a second conformance
regeneration; that is planned, not a mistake.

### M25.5 — prefilter + independence
`shared/policy/independence-rules.v1.json` (`format: 1, rule_version: 1`) with
`distinct_firsthand_origin` and `independent_system_artifact`. **Recommended
split** (recon Q5): the REDUCER validates only that `rule_version` is present
and non-empty; the PREFILTER (Rust-only, outside the vector contract)
evaluates the predicates against the artifact. Otherwise every conformance
vector's expected refusals become artifact-version-dependent.
Also: this phase touches `independence.rs`, so resolve the stale
`human_confirmed` refusal there.

### M25.6 — quota, health, failure surfacing
`runtime_health` vs `source_connection`/`source_health` are three different
tables and three different sentences (§86). The 24-hour blindness threshold
emits `coverage.assessed` then `coverage.gap`; recovery emits
`coverage.restored`. Scan/parse/extraction failures use `ingestion_failures`
and their own banner copy.

### M25.7 — the control surface
Titlebar is `src/app/Topbar.tsx` (35 lines, no testid on the root; the pause
goes between `<SyncBadge />` and `<CreateMenu />`). Panel work: lane toggles,
the global budget meter, the vault-filterable **Activity log** (the word
"ledger" stays reserved for the epistemic ledger). `ReviewPage.tsx` is the
M24.9 precedent for IPC + testids; note it hardcodes `REVIEWER = 'human:me'`
rather than reading `uiStore.actorId`. **The mock serves fixed runtime-DB
shapes and holds no budget logic** — a second budget engine in `mockIpc` is
the twin-implementation defect. uiStore mirrors the DB pause and should
**default to paused until the first read returns**.

### M25.8 — checkpoint collapse + soak
One git commit per applied M22 logical batch. The two-vault simulated day
asserts ≤20 ambient runs globally, exact arithmetic, vault isolation, zero
mtime spend, every route exercised, no automatic recovery spend, every failure
visible. **Copy demo-vault first** — its mtimes are assertion inputs elsewhere.

### M26, M27, M28
Plans and specs exist at `docs/superpowers/plans/2026-08-07-cerebro-m26…`,
`…m27…`, `…m28…` and the matching specs dated 2026-08-08. Do not start M26
until M25.8 is green: M26 is "agents on by default", and the entire argument
for that is the metering, budget, and visibility M25 builds. `PREDICATE_OWNERS`
in `policy/preconditions.rs` names `open_contradictions_addressed` as M27's.

---

## Working rules for a fresh context

- **Verify before you claim.** Run the gate; quote the counts. The last known
  green numbers are recorded in each phase's commit message.
- **The schema is the tripwire.** Two real ordering bugs in M25 were caught by
  foreign keys and CHECK constraints, not by tests. If SQLite refuses
  something, read it as a design answer before reaching for `PRAGMA`.
- **Never mirror a rule.** If a rule needs to exist in Rust and TS, it belongs
  in a shared artifact with a digest, or in the conformance vectors.
- **Held is a real answer.** Whenever the honest options are "assume consumed"
  or "assume pending", the third answer — hold it and ask the owner once — is
  the one this milestone keeps choosing. Follow it.
