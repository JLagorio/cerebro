# M29.53 — working the live audit: what is fixed, and what is left

**Branch** `m29-mermaid` · **Base** `988c56a` (the audit handoff) · **HEAD**
`69bdbfa` · nine commits, 39 files, +2560/−371

The audit raised 71 findings. **57 are closed, 6 were refuted, 8 are open** —
every one of those numbers driven in Chromium at HEAD rather than reasoned
(§3). Gate at HEAD: unit **3126 / 2 skipped** (187 files) · e2e **46** · lint,
typecheck, format clean.

> An earlier draft of this file said "46 closed / 5 refuted / 20 open". That
> was a tally, not a measurement, and it was wrong in both directions: the
> fixes closed 54 findings, three more turned out to have been closed already
> by M29.52 or by a side effect of this wave, and only 8 survive. The count
> below is the measured one.

Read §1 first. It is not a summary of the work — it is the thing that would
have cost the next session a day.

---

## 1. The appendix's verdicts are attached to the wrong findings

`2026-08-10-cerebro-m29-live-audit-findings.md` was generated by zipping the
probe agents' findings against the verifiers' records **in different orders**.
Inside each entry, `Surface` / `Repro` / `Measured` / `Suspected cause` belong
to the heading above them; `Re-measured by the verifier` and `Verifier's
reasoning` describe a completely different finding.

That makes the `Confirmed` / `Refuted` grouping correct and the **titles in
those sections wrong**. The full corrected mapping is now at the top of the
appendix itself. The two that matter most:

- **"Backspace deletes the ENTIRE mermaid block" is printed under _Refuted_
  and is in fact CONFIRMED** — one keystroke, 842 bytes to 700, four fences to
  three. Following the appendix's own grouping, the next session would have
  skipped the most destructive finding in the wave.
- **"One Escape closes Quick Open AND the dialog underneath" is printed under
  _Refuted_ and carries no verdict at all.** I drove it: it reproduces. Fixed.

The verifier text always names what it measured, so the mapping is recoverable
by reading rather than by guessing. It is written down now; do not re-derive it.

---

## 2. What is fixed, by mechanism

Nine commits, each one mechanism. The commit messages carry the measurements;
this is the index.

**`7fa27fe` — the keys and the chrome a diagram surface owns.** Eleven
findings, two mechanisms. Nothing inside a mermaid surface ever holds focus, so
every React `onKeyDown` carrying a gesture was on an element the keystroke
never passed through: Escape never exited the block's visual editor, Backspace
reached ProseMirror and took the whole diagram out of the document, and ⌘Z
after `+ Node` was a no-op because focus sat on the button that made the edit.
The editor claims Delete/Backspace/Escape at the **window**, in capture, so it
can stop the key before a host answers it — moving focus around could not have
done that, and measurement said so. Plus: the block routes ⌘Z to the document's
history explicitly, flushes an uncommitted source draft on unmount, both source
boxes stop only what the surrounding editor claims (a blanket stop killed the
app's own ⌘K), and double-click opens the editor.

The second mechanism: M29.51 made the overlays counter-scale but baked their
screen constants into the stored coordinate, which fixes them to the scale in
force at that instant. The drift is exactly `k·(s − s₀)`. The constants ride in
the transform now. Three affordances M29.51 never reached — the selection ring,
the connect handle, the editor's own focus ring — and the background click and
connect drop moved to the **viewport**, which is what a user reads as "the
canvas" (the editor's own box is 25.1% of it at rest, 6.7% at 39% zoom).

**`d28fc13` — a canvas that says what is wrong.** Every parse-error banner
printed the line number twice and dropped the clause that says what broke;
`summarizeRenderError` keeps it, condenses a 670-character grammar dump to the
token that failed, and answers an empty file with a sentence about the file.
A syntax error made in **visual** mode reported nothing at all. The code panel
buried the zoom cluster. Fit centred a quarter of the diagram under that panel.
The percentage chip threw the pan away. Auto-update OFF was reading as a
shredder.

**`72f2b58` — an export that carries the picture.** PNG export failed for
every diagram type mermaid draws with HTML labels, flowcharts included, because
a `<foreignObject>` taints the canvas: labels become real SVG `<text>` for the
raster path. Exports carry the app's own background now (73.4% of a dark-theme
PNG was transparent). Export falls back to the last-good svg instead of
refusing while a valid picture is on screen. And a copy crossing a mermaid
block carries the fence rather than the block's button labels.

**`95d5a1e` — a viewer that fits, stays put, and does not eat the document.**
One keystroke in the lightbox deleted the whole block. The readout lied and
there was no Fit. Wheel zoom ran away from the pointer; the pan was unclamped.
And a latent one it uncovered: the injected svg took an inline innerHTML prop
object, which React re-applies on **identity** — one pan frame would have
restored every stripped link target.

**`2f57da1` — a drag that moves the node, not the diagram.** The whiteboard's
primary gesture. `growViewBox` reports the screen-pixel shift its origin move
causes and the canvas cancels it with a pan; the geometry was never wrong.
Also: a viewport that changes size keeps its centre (opening a record panel left
0 of 2 nodes on screen), Escape closes the panel a record chip opened, and the
delete-view dialog stops claiming to remove a drawing it leaves on disk.

**`c058d4d` — chrome that fits its window, files with no debris, ELK
subgraphs.** Toolbars scroll instead of running off the page. The block's own
chrome wraps instead of painting 147px outside its card at the app's minimum
window. Appends keep the file's trailing newline; removing the layout override
takes the mapping it emptied. Under ELK a subgraph could never be opened at all
— the renderer names the cluster group `"[object Object]"` — so the binding
falls back to document order. Plus one text size per document, focus rings on
the hand-rolled buttons, and a selection ring with its own token.

**`5261647` — the visual editor is reachable without a mouse.** The full tab
ring of the diagram page was 29 stops and not one was inside the drawing. Nodes
are named buttons under a roving tabindex; focus selects, the arrows walk,
Enter renames, Delete works because there is finally a selection to read.

**`5c3ba95`** — two regressions the e2e suite caught from the two commits
before it. **`69bdbfa`** — Quick Open closing two layers on one Escape, the
code panel answering Escape with its own dismissal, the rename box and group
title no longer eating ⌘K, and full screen reachable from a block being edited.

---

## 3. What is left — all eight driven in Chromium at HEAD

Every claim below was re-driven with real input against `8d99d58` before it was
written down. Three findings that were listed here as "needs re-measuring" did
not reproduce and are closed:

| Closed by measurement | What the probe found |
|---|---|
| Tabbing to an overflowed toolbar button scrolls the content pane 100px sideways | after 22 Tab presses the ONLY scrolled element is the toolbar itself (239px); `whiteboard-view` stayed at x=236. The `overflow-x-auto` in `c058d4d` moved the nearest scrollable ancestor from the page pane to the strip |
| "Add record" drops the 4th record and beyond outside the canvas | five records, five nodes inside the viewport (x=872..1177 against a canvas that ends at 1280) |
| There is no undo on the whiteboard canvas | ⌘Z after placing a record returned the file to its two-line seed |

And the eight that DO reproduce:

**Needs a decision before it needs a patch.**

1. **The BlockNote hard-break corruption** (`wrong`, not ours). MEASURED at
   HEAD: `strategy/systems-map.md` 842 → 844 bytes on a node rename, and the
   prose line `…The flowchart below uses the` gains a trailing `\`. It is the
   document's markdown round-trip, not M29 — an inline source edit does it too
   — but M29 gives it a trigger a user hits daily. Needs an editor-area owner.
2. **A link badge clicked inside the full-screen dialog destroys it and
   navigates the whole app** (`rough`, but it is surface loss on a click that
   never asked for it). MEASURED: dialog count 1 → 0, scrims 0, doc title
   "Systems map" → "Phoenix cutover standup". `useOpenPath('in-place')` is
   deliberate for the inline block, where the doc IS the backdrop; in the
   dialog the backdrop is what gets replaced. Peek panel, or close-first — a
   product call.
3. **Below ~50% zoom the node toolbar covers the node under it** (`wrong`).
   MEASURED at 39%: `elementFromPoint` at the second node's own centre returns
   `mermaid-node-toolbar`, so shift-click multi-select on it is dead. The
   toolbar is 34 SCREEN px and the node gap is 40 PLANE px, so below ~85% they
   must collide somewhere; choosing above-vs-below by headroom cannot help
   once both sides are occupied. Wants a placement rule (flip to the side,
   or clamp into free space), which is a design call.
4. **Record chips repeat their node's label and cannot be grabbed**
   (`rough`). MEASURED: chip text "Rack layout sign-offDone" against node text
   "Rack layout sign-off"; 1762px² of overlap with the node's bottom edge; a
   real 200px drag by the chip moved the node 0px and opened nothing. One part
   of the original finding is now FALSE and should not be fixed: chips are
   NARROWER than their nodes today (176 vs 345, 229 vs 455), not 30-39px wider.

**Cheap, contained, and simply not done.**

5. **The four node popovers do not track their anchor through a wheel zoom.**
   MEASURED: five wheel steps to 161% moved the node by (−358, −214) and the
   shape palette by (0, +2.4), still open, now over an unrelated part of the
   diagram. The node toolbar and the link badges track their anchors since
   `7fa27fe`; `Popover` re-measures on window `resize` and its own
   ResizeObserver, neither of which a CSS-transform zoom fires. The fix belongs
   in `Popover` — a canvas-scale subscription — not in a fifth caller.
   (A BUTTON zoom dismisses the palette, because pressing the button is an
   outside press. Only the wheel reproduces it, which is why the first probe
   run's "pass" was thrown away.)
6. **The Add-record picker has no keyboard navigation.** MEASURED: 25 options,
   0 marked, `aria-activedescendant` null, and ArrowDown moves nothing. ⌘K's
   own scorer already does roving selection — copy that.
7. **Exports name a bundled webfont they do not embed.** MEASURED: a
   14,890-character exported svg names `'Instrument Sans'` in nine places and
   carries 0 `@font-face` rules, so every rasterised diagram is set in the
   fallback face against box geometry computed for another one.
8. **Focus restore after the dialog closes** lands on the ProseMirror root
   (`DIV`, text "Systems mapHow the demo produc…"), not on the "Open full
   screen" button it came from — `useFocusRestore` captures a node BlockNote
   later re-creates.

**Refused, with the measurement:** the refuted findings in §1's table.
The Ask AI toolbar is less obscuring than the text-selection toolbar that
ships; left-aligned diagrams and the illegible inline gantt were fixed by
M29.52; "Save as file… writes two files" is documented, deliberate and
test-locked; and a document ending in a mermaid block has no trailing paragraph
because **no** document in the vault does.

---

## 4. Conventions this wave held to

- Commits are `type(scope): sentence (M29.53)`. Next number is **M29.54**.
- Every fix carries its measurement in a comment at the site, in the tense the
  rest of this codebase uses: what was measured, what it was, what it is now.
- Every geometry fix has a test that can fail. jsdom reports 0×0 for every
  rect, so the new `with measurable rects` describes stub
  `getBoundingClientRect` — and the counter-scale, fit, clamp and pan-cancel
  cases were each run against the unfixed code first to watch them fail.
- `keys.ts` is the one place that decides which keystrokes the surrounding
  editor claims. It is expressed as what the EDITOR takes, not as a copy of
  App.tsx's shortcut list, so a new global shortcut works inside a source box
  the day it is added.
- The e2e suite caught two regressions this wave that 3100 unit tests did not.
  Run it (`PORT=5273 pnpm e2e` — check the port is free first).
