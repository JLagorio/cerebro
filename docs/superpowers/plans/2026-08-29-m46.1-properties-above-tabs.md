# M46.1 — Properties above the tabs Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reverse M45.6's central model. Property sections are GLOBAL — heading, groups and rest all render above the tab strip, on every tab — and a tab holds only the page body or a data-source view, exactly as Notion does.

**Architecture:** A subtraction, not an addition. `LayoutGroup.tab` and every mechanism built to scope sections by tab retire: the `LayoutTab` argument on `resolveLayout`, `layoutTabScope`, `canHoldSections`, `setGroupTab`, `addGroup`'s tab argument, "Move to tab…", the tab-scoped canvas, and the per-surface tab threading. The `properties` tab kind retires with them. What stays: the peek's tab strip (M45.6 Task 2 — correct and confirmed), the customizer's live tab strip, and view tabs.

**Spec:** `docs/superpowers/specs/2026-08-29-cerebro-m46-notion-parity-polish.md` §2.

---

## The ruling this implements (user, 2026-08-29)

> "sorry tabs are only for related data sources. fields shwo above. just like notion."

Confirmed against the live Notion app: the property strip renders above the tab strip
and shows on every tab; the tabs hold the page body ("Content") and linked data sources.
A property section never belongs to a tab.

## Decisions this plan locks

- **`LayoutGroup.tab` is deleted from the type, not deprecated.** A vault carrying the
  key — only reachable by someone who ran M45.6 locally, never by a user of a shipped
  build — parses tolerantly, ignores it, and drops it on the next Apply. The parse
  tolerance is the ONLY accommodation; no migration, no compatibility branch.
- **The `properties` tab kind retires.** A tab that shows the property stack is exactly
  what the correction forbids. `TAB_CONTENTS` becomes `overview | sections | view`. A
  vault carrying `content: properties` falls back to `overview` at the parse door, which
  is the existing unrecognised-kind rule — a tab is never dropped, because its id may be
  load-bearing.
- **`overview` means the page body.** With properties hoisted out of the tab, the
  Overview tab is Notion's "Content": the body editor and nothing else.
- **Property surfaces render the full stack above the strip on every tab**, including
  `sections` and `view` tabs. This deletes the M45.6 reachability rules (`DocSidePanel`'s
  exemption comment, the peek's no-property-bearing-tab arm) because the condition they
  guarded against cannot arise: the stack is always on screen.
- **The customizer canvas mirrors that order**: heading → property groups → rest → tab
  strip → the active tab's content. The canvas's tab strip keeps editing tabs; it no
  longer scopes sections, and the `+ Add section` button targets no tab.
- **`rest`, `heading`, and `flat` keep their global semantics** — they were already
  global, and the reason recorded in M45.6 (a per-tab `flat` double-renders a property)
  stands unchanged.

## File structure

| File | Change |
|---|---|
| `src/engine/types.ts` | delete `LayoutGroup.tab`; `TAB_CONTENTS` loses `properties` |
| `src/engine/schema.ts` | stop parsing/serializing `tab`; `properties` → `overview` at the door |
| `src/engine/layout.ts` | `resolveLayout(layout, fields)` — the `LayoutTab` argument and `showsOn` retire; `revealableFields` stays |
| `src/engine/typeCatalog.ts` | delete `layoutTabScope`, `tabBearsProperties` |
| `src/engine/layoutEdit.ts` | delete `setGroupTab`; `addGroup` loses its tab argument |
| `src/detail/RecordProperties.tsx`, `DocProperties.tsx` | drop the `tab` prop |
| `src/detail/DetailPanel.tsx`, `src/pages/DocPage.tsx` | stack renders above the strip on every tab; drop the scope and the reachability arms |
| `src/detail/LayoutCanvas.tsx` | property zones above the strip, always; `+` targets no tab |
| `src/detail/GroupEditorPopover.tsx` | delete "Move to tab…" |
| `src/detail/RecordTabs.tsx` | `TAB_KINDS` loses the Properties tile |
| `src/detail/LayoutEditorDialog.tsx` | `seedDraft` stops carrying `tab` |
| `e2e/layout-editor.spec.ts` | the M45.6 per-tab journey is reversed into an above-the-strip assertion |

## Tasks (each = TDD: failing test → red → implement → green → commit)

### Task 1: Engine — sections are global again
Delete `LayoutGroup.tab`, the `LayoutTab` seam, `layoutTabScope`, `tabBearsProperties`, `setGroupTab`, and `addGroup`'s tab argument. `TAB_CONTENTS` loses `'properties'`; the parse door maps a stored `properties` to `overview` (never drops the tab). Keep `revealableFields`. Tests: a `tab:` key in YAML is ignored and does not round-trip; `content: properties` parses to `overview` with the tab intact; every M45.6 tab-scoping test is deleted, not weakened — and say so in the commit body. Commit: `feat(engine): a layout section belongs to the record, not to a tab (M46.1)`.

### Task 2: The record surfaces put properties above the strip
`DocPage` and `DetailPanel` render heading + stack ABOVE the tab strip, on every tab, and the tab renders only its own content. Drop the `tab` prop from `RecordProperties`/`DocProperties`, the tab scope, the peek's no-property-bearing-tab arm, and `DocSidePanel`'s exemption comment (the condition it guarded cannot arise). Tests: a `view` tab and a `sections` tab both show the full property stack above the strip; the body renders only on `overview`. Commit: `feat(detail): a record's properties stand above its tabs (M46.1)`.

### Task 3: The customizer mirrors the page
Canvas order: heading → groups → rest → tab strip → active tab's content. `+ Add section` targets no tab; "Move to tab…" is deleted; `seedDraft` stops carrying `tab`; `TAB_KINDS` loses the Properties tile. The three index-coupling fixes from M45.6 stay (they are correct regardless of filtering) but their tab-filtering rationale comments must be re-truthed. Tests: the canvas shows every section on every tab; the Apply payload carries no `tab:`. Commit: `feat(detail): the customizer puts properties above the tabs (M46.1)`.

### Task 4: e2e + gate + write-back
Reverse the M45.6 journey: open the customizer → add a section → Apply → the section shows above the strip on EVERY tab, including a view tab. Keep the peek-strip assertion (defect 1 stays fixed). Full gate (unit, coverage ratchet UP only, lint, typecheck, format, e2e on a FREE port — check with `lsof` first; 5173 is permanently held). Write-back appended here; push to PR #19. Commit: `test(e2e): properties stand above the tabs, on every tab (M46.1)`.

## Self-review notes
- This slice DELETES more than it adds. Every deletion must take its tests and its comments with it — a test that no longer describes the product is worse than no test.
- M45.6's write-back stays as the historical record of a superseded ruling; append a line noting M46.1 reversed it rather than rewriting it.
- Not in slice: any polish (M46.2+). Behavior order only.
