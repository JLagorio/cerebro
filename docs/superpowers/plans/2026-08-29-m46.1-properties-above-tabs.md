# M46.1 — Properties above the tabs Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reverse M45.6's central model. Property sections are GLOBAL — heading, groups and rest all render above the tab strip, on every tab — and a tab holds only the page body or a data-source view, exactly as Notion does.

**Architecture:** A subtraction, not an addition. `LayoutGroup.tab` and every mechanism built to scope sections by tab retire: the `LayoutTab` argument on `resolveLayout`, `layoutTabScope`, `canHoldSections`, `setGroupTab`, `addGroup`'s tab argument, "Move to tab…", the tab-scoped canvas, and the per-surface tab threading. The `properties` tab kind retires with them. What stays: the peek's tab strip (M45.6 Task 2 — correct and confirmed), the customizer's live tab strip, and view tabs.

**Spec:** `docs/superpowers/specs/2026-08-29-cerebro-m46-notion-parity-polish.md` §2.

---

## The ruling this implements (user, 2026-08-29)

> "sorry tabs are only for related data sources. fields shwo above. just like notion."

Confirmed against the live Notion app: the property strip renders above the tab strip
and shows on every tab; the tabs hold the page body ("Content") and linked data sources.
A property section never belongs to a tab.

## Decisions this plan locks

- **`LayoutGroup.tab` is deleted from the type, not deprecated.** A vault carrying the
  key — only reachable by someone who ran M45.6 locally, never by a user of a shipped
  build — parses tolerantly, ignores it, and drops it on the next Apply. The parse
  tolerance is the ONLY accommodation; no migration, no compatibility branch.
- **The `properties` tab kind retires.** A tab that shows the property stack is exactly
  what the correction forbids. `TAB_CONTENTS` becomes `overview | sections | view`. A
  vault carrying `content: properties` falls back to `overview` at the parse door, which
  is the existing unrecognised-kind rule — a tab is never dropped, because its id may be
  load-bearing.
- **`overview` means the page body.** With properties hoisted out of the tab, the
  Overview tab is Notion's "Content": the body editor and nothing else.
- **Property surfaces render the full stack above the strip on every tab**, including
  `sections` and `view` tabs. This deletes the M45.6 reachability rules (`DocSidePanel`'s
  exemption comment, the peek's no-property-bearing-tab arm) because the condition they
  guarded against cannot arise: the stack is always on screen.
- **The customizer canvas mirrors that order**: heading → property groups → rest → tab
  strip → the active tab's content. The canvas's tab strip keeps editing tabs; it no
  longer scopes sections, and the `+ Add section` button targets no tab.
- **`rest`, `heading`, and `flat` keep their global semantics** — they were already
  global, and the reason recorded in M45.6 (a per-tab `flat` double-renders a property)
  stands unchanged.

## File structure

| File | Change |
|---|---|
| `src/engine/types.ts` | delete `LayoutGroup.tab`; `TAB_CONTENTS` loses `properties` |
| `src/engine/schema.ts` | stop parsing/serializing `tab`; `properties` → `overview` at the door |
| `src/engine/layout.ts` | `resolveLayout(layout, fields)` — the `LayoutTab` argument and `showsOn` retire; `revealableFields` stays |
| `src/engine/typeCatalog.ts` | delete `layoutTabScope`, `tabBearsProperties` |
| `src/engine/layoutEdit.ts` | delete `setGroupTab`; `addGroup` loses its tab argument |
| `src/detail/RecordProperties.tsx`, `DocProperties.tsx` | drop the `tab` prop |
| `src/detail/DetailPanel.tsx`, `src/pages/DocPage.tsx` | stack renders above the strip on every tab; drop the scope and the reachability arms |
| `src/detail/LayoutCanvas.tsx` | property zones above the strip, always; `+` targets no tab |
| `src/detail/GroupEditorPopover.tsx` | delete "Move to tab…" |
| `src/detail/RecordTabs.tsx` | `TAB_KINDS` loses the Properties tile |
| `src/detail/LayoutEditorDialog.tsx` | `seedDraft` stops carrying `tab` |
| `e2e/layout-editor.spec.ts` | the M45.6 per-tab journey is reversed into an above-the-strip assertion |

## Tasks (each = TDD: failing test → red → implement → green → commit)

### Task 1: Engine — sections are global again
Delete `LayoutGroup.tab`, the `LayoutTab` seam, `layoutTabScope`, `tabBearsProperties`, `setGroupTab`, and `addGroup`'s tab argument. `TAB_CONTENTS` loses `'properties'`; the parse door maps a stored `properties` to `overview` (never drops the tab). Keep `revealableFields`. Tests: a `tab:` key in YAML is ignored and does not round-trip; `content: properties` parses to `overview` with the tab intact; every M45.6 tab-scoping test is deleted, not weakened — and say so in the commit body. Commit: `feat(engine): a layout section belongs to the record, not to a tab (M46.1)`.

### Task 2: The record surfaces put properties above the strip
`DocPage` and `DetailPanel` render heading + stack ABOVE the tab strip, on every tab, and the tab renders only its own content. Drop the `tab` prop from `RecordProperties`/`DocProperties`, the tab scope, the peek's no-property-bearing-tab arm, and `DocSidePanel`'s exemption comment (the condition it guarded cannot arise). Tests: a `view` tab and a `sections` tab both show the full property stack above the strip; the body renders only on `overview`. Commit: `feat(detail): a record's properties stand above its tabs (M46.1)`.

### Task 3: The customizer mirrors the page
Canvas order: heading → groups → rest → tab strip → active tab's content. `+ Add section` targets no tab; "Move to tab…" is deleted; `seedDraft` stops carrying `tab`; `TAB_KINDS` loses the Properties tile. The three index-coupling fixes from M45.6 stay (they are correct regardless of filtering) but their tab-filtering rationale comments must be re-truthed. Tests: the canvas shows every section on every tab; the Apply payload carries no `tab:`. Commit: `feat(detail): the customizer puts properties above the tabs (M46.1)`.

### Task 4: e2e + gate + write-back
Reverse the M45.6 journey: open the customizer → add a section → Apply → the section shows above the strip on EVERY tab, including a view tab. Keep the peek-strip assertion (defect 1 stays fixed). Full gate (unit, coverage ratchet UP only, lint, typecheck, format, e2e on a FREE port — check with `lsof` first; 5173 is permanently held). Write-back appended here; push to PR #19. Commit: `test(e2e): properties stand above the tabs, on every tab (M46.1)`.

## Self-review notes
- This slice DELETES more than it adds. Every deletion must take its tests and its comments with it — a test that no longer describes the product is worse than no test.
- M45.6's write-back stays as the historical record of a superseded ruling; append a line noting M46.1 reversed it rather than rewriting it.
- Not in slice: any polish (M46.2+). Behavior order only.

---

## Slice write-back (2026-08-29)

Landed: T1 1b70327 + 0f88358 · T3 8de1da0 · T2 f8687a3 (the customizer landed
before the surfaces; the engine seam was already gone, so neither task could
see the other's half-state) · T4 eac0dfb · review rounds e5a6ffe (five
falsified "fourth arm" comments, renamed to name the KIND rather than the
ordinal — an ordinal that tracks a list's length is a comment that rots the
next time the list changes, which is how these five went stale) and 6bd8404
(the popover's second Add-section door, six cases) · this write-back. Gate:
4336 unit passed, 8 skipped (4344 in 239 files) / e2e 114 on port 5373 —
unchanged from M45.6, because its journey was REPLACED rather than added to
(5173 is permanently held by another worktree) / lint, typecheck and format
clean. No Rust in the slice — `git diff 36a1f43..HEAD --stat -- src-tauri` is
empty, so `cargo test` was not part of this gate.

**Coverage fell, was REPAID with real tests, and the ratchet did not move in
either direction.** The slice first measured 82.26/84.59/76.68/82.26 against a
floor of 82.3/84.6/76.7/82.3 — four red lanes. It was not a regression, and
the arithmetic is worth keeping, because this is the shape a subtraction slice
makes:

| | covered | total | UNCOVERED |
|---|---|---|---|
| statements | 53987 → 53823 (−164) | 65587 → 65426 (−161) | 11600 → 11603 (**+3**) |
| functions | 3071 → 3058 (−13) | 4000 → 3988 (−12) | 929 → 930 (**+1**) |
| branches | ~16932 → 16829 (−103) | 20001 → 19894 (−107) | 3069 → 3065 (**−4**) |

~161 statements, 12 functions and 107 branches were deleted that had been
essentially fully covered, while the ABSOLUTE uncovered count barely moved
(+3, +1) and branches strictly improved. Removing 100%-covered code from a base
at 82.31% drags the ratio down on its own. **Lowering the floor was refused**
("ratchets only tighten"), and the deficit was referred up rather than papered
over from inside the slice.

The answer came back as tests, which is the only currency that should settle
this: 6bd8404 gave the group editor's FOOTER Add-section door its six cases
back (`GroupEditorPopover.tsx` 96.30% → 98.52% statements, 89.23% → 92.48%
branches). The final figures, measured twice:

| | run 1 | run 2 | floor | margin | headroom |
|---|---|---|---|---|---|
| statements | 53846/65426 = 82.300614% | identical | 82.3 | +0.000614 pp | **0.40 statements** |
| lines | 53846/65426 = 82.300614% | identical | 82.3 | +0.000614 pp | **0.40 lines** |
| functions | 3059/3988 = 76.705115% | identical | 76.7 | +0.005115 pp | **0.20 functions** |
| branches | 16847/19903 = 84.645531% | 16846/19901 = 84.649013% | 84.6 | +0.0455 / +0.0490 pp | ~9 branches |

All four clear, and the gate exits 0 both times — but the rounded display
(`82.3` against a floor of `82.3`) is not what clears it; the sixth decimal is.
**The ratchet was therefore NOT raised.** The ruling licensed a raise only if
the figures cleared "with margin to spare", and two of them clear by less than
a single unit: one more uncovered statement anywhere in `src/` puts statements
and lines back under, and one more uncovered function puts functions under.
Raising the floor to a number the next commit breaks is the same mistake as
lowering it to a number this one passes — a ratchet is a floor under work
already banked, not a bet on the next slice.

The two-run noise profile is M45.x's, unchanged and now three slices old:
statements, lines and functions report the identical numerator AND denominator
across consecutive runs (53846/65426, 3059/3988), so their sub-unit margins are
real rather than measurement luck, and CI reproduces them exactly from the same
bytes. Branches moves both halves run to run (16847/19903 → 16846/19901, a
0.003 pp swing) — it is the only axis that drifts, and the only one with real
headroom, which is why it keeps the wider margin.

Recorded so a later reader does not over-read them: **the `'tab' in g`
assertions in `LayoutCanvas.test.tsx:585` and `GroupEditorPopover.test.tsx:810`
pin the WRITE CONTRACT, not a draft-level guard.** `serializeLayoutConfig`
rebuilds every group as `{id, name, fields}` unconditionally, so a stray `tab`
key on a draft group cannot reach the payload however it got there — mutating a
minted group to re-add one fails nothing. They are worth keeping as a pin on
the serializer's shape, but the behavioral guard against re-scoping is
`LayoutCanvas.test.tsx`'s `properties stand above the tabs, on every tab
(M46.1)` describe, which is the case that goes red if a tab filter ever comes
back.

**The deletion tally.** `src/` moved +823 / −1986 across 29 files — a net
−1163 lines — of which the test suites are +436 / −1143. The slice removed
roughly 2.4 lines for every line it added, and 43 M45.6 tests went with the
model they described (21 in T2, 18 deleted + 2 rewritten in T3, the rest in
T1's engine suites). None were weakened: a test that no longer describes the
product is worse than no test.

**Decisions:**

- **A retired Properties tab becomes FREE TEXT, not a second body** (0f88358).
  `TAB_CONTENTS` lost `properties`, and the parse door had to send a stored
  `content: properties` somewhere. The obvious landing — the unrecognised-kind
  rule's `overview` — is wrong: `overview` IS the page body, and a type whose
  vault already carried a Properties tab beside its Overview would parse into
  TWO overview tabs, i.e. two body editors on one file, each writing the same
  bytes. `sections` is the honest destination: per-record free text, an empty
  block until someone types in it, and it can stand beside an Overview without
  either one lying about what it edits. The tab is never DROPPED, because its
  id may be load-bearing (a saved selection, a link).
- **`LayoutGroup.tab` is deleted from the type, not deprecated.** A vault
  carrying the key is only reachable by someone who ran M45.6 locally; it
  parses tolerantly, is ignored, and is dropped on the next Apply. That parse
  tolerance is the only accommodation — no migration, no compatibility branch.
- **The M45.6 reachability machinery is GONE because the condition cannot
  arise.** The peek's `propertiesOrphaned` short-circuit, `DocPage`'s
  `tabScope`, `DocSidePanel`'s "surface of last resort" comment and
  `InboxPage`'s "no tab passed, on purpose" paragraph all existed to answer one
  question: where does a record's property stack render when every tab it
  declares refuses to hold one? Under M46.1 the stack is above the strip on
  every tab, so it is always on screen and there is nothing to strand. Deleting
  the guards was mandatory, not optional — a guard against an impossible state
  is a comment that will be believed by the next reader.
- **The heading strip's expander is now offered on every tab.** Its fold is
  the ONE gate left on the stack; gating the toggle by tab kind would strand
  the record's sections behind a strip with no way to open.
- **`RecordProperties` stays keyed by PATH, not by tab** — the M45.6 reason
  survives its model. Every piece of state it owns (reveal expander,
  add-property flyout, a drag in flight) is about the record's stack, and it is
  the same stack on every tab; a tab-keyed remount would discard a flyout the
  user opened and discard no stale state, because there is none.

**The DocPage tab-strip move, and what it costs.** The strip was a `flex-none`
sibling pinned ABOVE the scroll container. There is no way to put properties
above it from there, so it moved into the content column, under the stack.
It scrolls with the record it describes now — which is Notion's order, the
peek's order and the customizer canvas's order — and it costs exactly one
thing, recorded in place at the JSX: **reading an inline diff swaps the whole
canvas out, the strip included, so tabs are unavailable until the diff is
closed.** Accepted deliberately. The alternative is a strip pinned above a
stack it is supposed to sit below, which is the defect this slice exists to
fix. e2e now reaches the strip by scrolling rather than by finding it at a
fixed offset.

**e2e:** the M45.6 journey (`dc67e50`) is reversed rather than trimmed. Its
first claim survives whole — the peek carries the tab strip, M45.6's defect 1,
still fixed and still proven — and its second is replaced by the M46.1 truth:
mint a section in the customizer, put `owner` in it, swap the canvas to Epic's
**view** tab (the exact kind M45.6 said could hold no properties) and watch
only the tail change, then Apply and find the section on both tabs of both
surfaces, above the strip by `compareDocumentPosition` rather than by testid
order, with a Type doc that carries no `tab:` anywhere. Two falsified
assertions elsewhere died with their comments: M45.5's
`order.slice(0,2) === ['heading','tabs']` (the canvas order is
`heading → sections → rest → tabs → content` now — the user's "tabs are on top"
defect is still fixed, the strip just moved further down), and
`view-tabs.spec.ts`'s `page-properties → toHaveCount(0)`, which asserted M45.4's
"the tab IS the content" ruling that this slice reverses. That spec's rescan
trigger no longer hops to Overview to find a property row, because there is one
right there on the view tab — which is the reversal, stated as a journey step.

**Known limitations / honest omissions:**

- **The coverage floor is cleared by less than one statement.** Settled, not
  open — but the margin is a fact the next slice inherits, and it is the reason
  the ratchet stayed put. The first thing to touch `src/` will move these
  numbers, in whichever direction it deserves to.
- **`e2e/view-tabs.spec.ts` now dismisses the status popover with Escape**
  rather than riding the tab press through its dismiss, because the press it
  used to ride is gone. If that popover ever closes on selection, the Escape
  becomes a no-op rather than a failure — a silent softening, recorded here.
- **Carried, unchanged:** the canvas still does not live-embed a view tab
  (M45.4's weight-without-fidelity ruling), and the Simple tile still reseeds
  `[Overview]` without a confirm.
