[package]
name = "cerebro"
version = "0.1.0"
description = "Cerebro — files-first work management"
edition = "2021"

[lib]
name = "cerebro_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["preserve_order"] }
serde_yaml = "0.9"
walkdir = "2"
notify = "6"
chrono = "0.4"
trash = "5"
# Loopback MCP endpoint (M6). tiny_http over axum on purpose: we serve three
# JSON-RPC methods on 127.0.0.1, which needs a socket and a body parser, not
# an async web framework and its ~100 transitive crates.
tiny_http = "0.12"
rand = "0.8"
# stdio connector approval keys (PR #5 security review): the ledger stores
# SHA-256 digests of the env-bearing fingerprints, never the strings.
sha2 = "0.10"
